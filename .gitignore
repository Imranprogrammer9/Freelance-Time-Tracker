*.iml
.gradle
/local.properties
/.idea
.DS_Store
build/
/captures
.externalNativeBuild
.cxx
.kotlin/
local.properties
*.keystore
!debug.keystore
*.jks
/app/release
.session/

# Firebase config — app-specific, not committed to the kit
google-services.json

# RevenueCat / Google Cloud service-account key (credential — never commit)
revenuecat-key.json
*-key.json

# Ruby / Bundler
/vendor/bundle
.bundle/
