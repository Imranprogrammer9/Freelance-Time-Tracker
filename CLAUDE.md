# Project: ShipKaro Android Kit (a.k.a. NowKit)
> Last updated: 2026-06-05 (BRAND RENAME — product is now **NowKit** (was ShipKit; "ShipKit" too crowded on Google/Play/npm). Explored verb+kit options — every common one taken (ShipKit/BuildKit/LaunchKit/SoloKit/AppKit/FastKit/SpeedKit all collide or SEO-buried). NowKit + Quickit were the only clean web scans; picked **NowKit** (crisp pronunciation, "ship now" energy). Renamed across `../shipkit-site` (19 files: metadata/OG, MetallicMark `/shipkit`→`/nowkit`, Header, CTAs, Footer, Comparison heading + `shipkit` data field, FAQ, README, package name) — "by shipkaro" attribution + logo untouched. Build green, pushed to `shipkit-site` main (commit `2b44638`). New 500×500 Google-profile avatar `public/nowkit-avatar.png` rendered from the live MetallicMark spec via headless Chrome (`scripts/nowkit-avatar.html` generator). Pending manual: GitHub repo rename `shipkit-site`→`nowkit-site`, Vercel project + subdomain, domain grab (`nowkit.dev`), trademark/Play scan. The GitHub repo, Vercel URL `shipkit-site.vercel.app`, and `/kit-*` slash commands keep the `shipkit`/`kit` names for now — only the brand display name changed.)
> Earlier 2026-06-05 (Phase 7 site Day 2 — massive design pivot. New default `/` route = OSForms-inspired dark + grid + chrome wordmark + phosphor-green brand accent. Old Navy + Gold theme preserved at `/sleek`. Three palette experiments live at `/forest`, `/neon`, `/orchid` (CSS-skin scope on shared components). New `MetallicMark` typographic logo `/shipkit▌` types char-by-char on first load w/ blinking phosphor cursor. Hero terminal converted to line-by-line animated `TerminalTyper` w/ blinking cursor. FAQ accordion → single-open w/ smooth grid-rows expand. Sticky header fix (overflow-x-hidden was killing it). Custom rAF smooth-scroll easing on nav anchor clicks. Site-wide phosphor-glow card hovers + ambient phosphor-blob bg drift + 120s grid drift + 14s sparkle spin + 1.05s cursor blink. All GitHub OAuth refs stripped (paid kit, not open-source) — CTAs rewritten as "Get instant access — $29 / Get ShipKit — $29 / See what's inside ↓". `prefers-reduced-motion` honoured across every animation.)

## What This Is
A production-ready **pure Android native** (Kotlin + Jetpack Compose) starter kit that lets indie developers skip auth, paywall, analytics, and launch plumbing and ship a Play Store app fast. Primary use: handed to **ShipKaro Weekend** cohort attendees on Day 1 so live sessions focus on app logic, not boilerplate. Also sold standalone (ShipFast model: one-time price, lifetime updates) via a marketing landing page. iOS native variant is a future, separate effort — not in scope now.

## Context & References
- shipkaro.dev — community (300+), Wajahat Karim. Lists "The Kit" as launching soon.
- 1dayapp.shipkaro.dev — The Weekend cohort. Stack taught: Supabase, RevenueCat, PostHog, Firebase, Fastlane, Stitch/Figma.
- shipkaro.dev/mobile-docs — OLD KMM kit (reference ONLY, rebuild from scratch, pure Android). Feature parity target + docs IA reference.
- Comparable products studied: shipfa.st, swiftstarterkits.com, flutterfasttemplate.com (positioning, pricing, page structure).

## Tech Stack
- Language/UI: Kotlin, Jetpack Compose, Material 3, Navigation Compose 2.8 (type-safe routes; Nav3 deferred — see Decisions)
- Auth: Supabase (email + Google), Firebase Auth (toggle-able alternative)
- Paywall: RevenueCat
- Analytics: PostHog + Firebase Analytics + Crashlytics
- Backend: Supabase (Firebase optional)
- Launch/CI: Fastlane, GitHub Actions
- App config: runtime feature flags to toggle auth/paywall/modules
- Architecture: **single `app` module, package-by-feature** (no multi-module, no build-logic/buildSrc)
- Base template: cortinico/kotlin-android-template — keep ONLY `app` module; delete library-android / library-kotlin / library-compose
- Tooling (from cortinico): Gradle Kotlin DSL, version catalog, detekt + ktlint, GitHub Actions CI
- DI: **Koin** (no Hilt/KSP — simpler for beginner + AI vibe-coding audience)
- Patterns (from Drjacky/MVVMTemplate, flattened): MVVM, `NetworkResponse` sealed result, Navigation 3 type-safe, Material 3 + edge-to-edge. Drop build-logic, RxJava, KSP.
- Networking + persistence: **Retrofit + Room + DataStore** (max tutorial/AI coverage for beginners; veto window open)

## Project To-Do List
Check off as completed. Update the date when marking done.

### Phase 0: Foundation & Scaffolding  ✅ DONE 2026-05-18 (assembleDebug green)
- [x] Single `app` module scaffold (wrapper bootstrapped from cortinico template, no library modules)
- [x] Gradle Kotlin DSL + version catalog (AGP 8.5.2, Kotlin 2.0.21, Gradle 8.14.5)
- [x] Package-by-feature source layout (core/ + feature/*)
- [x] Koin DI setup (coreModule + dataModule + featureModule)
- [x] Networking layer: Retrofit + `NetworkResponse` sealed result + `safeApiCall`
- [x] Persistence: Room (`KitDatabase`) + DataStore (`SettingsRepository`)
- [x] Base theme + Material 3 + dynamic color + light/dark/system + edge-to-edge
- [x] Type-safe route graph (Navigation Compose 2.8 — see deviation note)
- [x] Code-config (`KitConfig`, compile-time) + runtime app-config contract (`RemoteAppConfig`)
- [x] Localization-ready: externalized strings, en + ur, per-app `LocaleManager`, locales_config
- [ ] Wire detekt to `:app` (currently root-only → detekt NO-SOURCE) — moved to Phase 5 CI

### Phase 1: Component Library + Design System (Swift-catalog parity)  ✅ DONE 2026-05-20 (assembleDebug green)
REORDERED to first — components are building blocks; auth/paywall/settings screens consume them. Building screens before components = double work.
- [x] `designsystem/foundation/` — Spacing, Shape, Elevation tokens + `KitTheme` ergonomic accessor
- [x] Icons via compose-icons lib `br.com.devsrsouza.compose.icons:{pack}:1.1.1` + **material-icons-extended retained as default**. KitIcons interface + 3 packs (Material default, Feather, Tabler). LocalKitIcons CompositionLocal. Per-callsite override supported. Other packs commented opt-in in version catalog.
- [x] `designsystem/components/` — KitButton (primary/secondary/text/loading), KitTextField, KitPasswordField, KitCard, KitListItem, KitBottomSheet, KitDialog, KitChip + KitFilterChip, KitAvatar, KitBanner
- [x] `designsystem/state/` — KitLoadingState / KitInlineLoading / KitEmptyState / KitErrorState / KitSuccessState (full-screen + inline via `fullScreen` param)
- [x] `designsystem/onboarding/` — KitOnboardingPager + KitPageIndicator
- [x] `designsystem/settings/` — SettingsSection, ToggleRow, NavRow, AccountRow, DangerRow, LegalLinks, SettingsDivider
- [x] **2026-06-02 SwiftStarterKits parity expansion (20 components)** — Snackbar w/ 4 severity styles (KitSnackbarController + KitSnackbarHost, Light-inspired), PullToRefresh, FAB, BottomNavScaffold + BottomNavBar, Shimmer modifier + Skeleton + SkeletonListItem, Slider, Stepper, SegmentedButton, DatePicker (modal) + InlineDatePicker, DropdownMenu (generic), SearchField, OtpField, SwipeableRow, LinearProgress + IndeterminateLinearProgress, DisclosureGroup, LazyGrid + AdaptiveLazyGrid, Badge, RatingBar, WebView (embedded, JS-off by default). Util classes: EmailLauncher (`mailto:`) + KitHaptics (`rememberKitHaptics`).
- [x] **2026-06-01 In-app showcase screen** — `feature/catalog/ComponentsCatalogScreen.kt` provides live previews of every component (12 sections after the 2026-06-02 pass: Buttons, Inputs, Chips, Cards/Lists, Banners, Avatars, Paywall pieces, State views, Settings rows, Dialogs/Sheets, Snackbars, Pickers, List patterns, Progress & shimmer, Badges & rating, Scaffolds, Utilities, Ops sheets). Reachable from Home → "Browse kit components".

### Phase 2: Auth + Account (Play compliance core) — built ON Phase 1 components  ✅ DONE 2026-05-21 (assembleDebug green)
- [x] Supabase auth: email/password + Google sign-in (Credential Manager primary + OAuth deeplink fallback)
- [x] Firebase Auth alternative (KitConfig.AUTH_PROVIDER toggle; Stub default, no creds needed to build)
- [x] Session handling + auth-gated navigation (SessionState Flow → nav guard redirects SignedOut → SignIn)
- [x] Settings screen (theme picker, account row, sign-out, delete account, legal links)
- [x] Account deletion + in-app data deletion flow (Play mandatory) — repo.deleteAccount + DataStore.clearAll + db.clearAllTables
- [x] Toolchain upgraded for supabase-kt 3.6.0: AGP 8.5.2→8.9.2, Kotlin 2.0.21→2.2.21, KSP 2.2.21-2.0.4, Room 2.6.1→2.7.2, compileSdk 34→36, Compose BOM 2024.09.02→2025.06.00. Targetsdk stays 35.

### Phase 3: Monetization — built ON Phase 1 components  ✅ DONE 2026-05-21 (assembleDebug green)
- [x] RevenueCat SDK wiring (`com.revenuecat.purchases:purchases:9.23.1`, native — no KMP) + entitlement checks + restore
- [x] `core/billing/PurchaseManager` — ported from KMM starter's manager pattern; extended w/ offerings + `purchase()`. isPremium/customerInfo/currentOffering StateFlows. Builds w/o API key (no-ops).
- [x] Paywall UI (hard + soft variants) — custom `PaywallScreen` on Phase 1 components (NOT RC's prebuilt `purchases-ui`). `KitConfig.PAYWALL_MODE` SOFT(skip)/HARD(blocking).
- [x] Onboarding → paywall combo flow — nav chains Onboarding → Auth → Paywall → Home; premium users auto-skip paywall.
- [x] BillingErrorCode enum + localized error mapping (en + ur); paywall strings externalized.

### Phase 4: Analytics + Ops  ✅ DONE 2026-05-21 (assembleDebug green)
- [x] PostHog events + Firebase Analytics — `AnalyticsManager` (ported from KMM starter, single native class)
- [x] Crashlytics crash reporting — `AnalyticsManager.logError` (always on, not gated by analytics toggle)
- [x] Remote config + feature flags — `FirebaseRemoteAppConfig` impl of `RemoteAppConfig`; `KitConfig.REMOTE_CONFIG_PROVIDER` (LOCAL default / FIREBASE)
- [x] Push notifications (FCM) + deep links / app links — `KitMessagingService` + `KitNotifications` channel; POST_NOTIFICATIONS perm; manifest service + https App Links intent-filter
- [x] Force / soft update gate (kill switch) — `UpdateManager` (reads min_supported_version / latest_version from RemoteAppConfig) + `UpdateGate` composable wrapping nav host
- [x] In-app review prompt — `InAppReviewManager` (ported from KMM, Play In-App Review API; FakeReviewManager in debug); Settings → About → Rate row
- [x] Maintenance mode + backend changelog (added 2026-05-22) — `UpdateManager` reads `maintenance_mode`/`maintenance_message`/`latest_version_name`; `ChangelogManager` parses `app_changelog` JSON from `RemoteAppConfig`; `UpdateGate` rewritten: maintenance screen → force update sheet → soft update sheet → auto "What's New" sheet
- [x] In-app changelog UI (added 2026-05-22) — `ChangelogScreen` (Settings → About → What's new) + version-bump "What's New" popup via DataStore `lastSeenVersionCode` (fresh install suppressed)

### Phase 5: Launch Tooling  ✅ DONE 2026-05-21 (assembleDebug + detekt green)
- [x] Fastlane: Play Store submission lane (internal track) — `fastlane/Fastfile` `internal` + `promote` lanes
- [x] Fastlane: screenshot generation + metadata — `screenshots`/`metadata` lanes + `fastlane/metadata/android/en-US/` structure
- [x] GitHub Actions CI — `ci.yml` (detekt + tests + assembleDebug) + new `release.yml` (signed AAB + Play internal upload)
- [x] Package-name refactor — `refactorPackage` Gradle task in `app/build.gradle.kts` (ported from KMM starter, adapted single-module). `./gradlew refactorPackage -PnewAppId=… -PnewAppName=…`
- [x] Changelog generator — `CHANGELOG.md` (Keep-a-Changelog seed) + references the changelog Claude skill
- [x] Privacy policy + terms templates — `legal/privacy-policy-template.md` + `legal/terms-template.md`
- [x] Play Data Safety mapping doc — `legal/play-data-safety.md` (per-SDK collection mapping)
- [x] ASO assets — `fastlane/metadata/` listing-copy templates double as ASO listing; full ASO playbook referenced to docs site
- [x] detekt wired to `:app` (was Phase 0 deferred) — config tuned for Compose (Long*/Cyclomatic thresholds, ignoreAnnotated Composable); `Routes.kt`→`Route.kt`

### Phase 6: Demo App + Design Pass  ✅ DONE 2026-05-21 (assembleDebug + detekt green)
Split into 6a (design pass) + 6b (demo). Model CHANGED mid-phase — see Decisions.
**6a — design pass:**
- [x] Brand recolor — `Color.kt` → shipkaro purple (`BrandPrimary #7C3AED`); `dynamicColor` default false; `ic_shipkaro_mark` logo vector
- [x] Component polish — settings rows w/ tinted icon-chips + grouped surfaces; new `KitPricingCard` / `KitFeatureRow` / `KitBulletRow`; `KitIcons` +palette/language/star/shield
- [x] Screen redesigns — Onboarding (pager + CTA), Paywall (tiers + benefits + fine print), Auth (mark + or-divider), Settings (grouped + bottom-sheet pickers)
**6b — demo:**
- [x] `WelcomeScreen` — kit entry placeholder: logo + kit info + "Launch Demo" button (gated `SAMPLE_FEATURE_ENABLED`)
- [x] `feature/demo/` self-contained subtree — `DemoNavHost` (splash→onboarding→paywall→auth→habits→settings), `DemoRoute`, `DemoModule`
- [x] Habit Tracker functionality — demo-owned Room (`DemoDatabase`), `HabitRepository` (seeds samples), list/add screens, streak + done-today, free 3-habit cap → paywall
- [x] Demo REUSES real onboarding/auth/paywall/settings screens with demo-nav callbacks (real auth, not fake)
- [x] `KitNavHost` stripped to `Welcome → Demo`; deleted Phase 0 throwaways `feature/home` + `feature/sample`
- [x] Deletion = 3 micro-steps: drop Welcome demo button, remove `demoModule` line in AppModules.kt, delete `feature/demo/`

### Phase 7: Sales + Docs Website  (web project — SEPARATE repo, not this Android repo)
> Full plan: `.session/phase7-sales-and-docs-plan.md` (git-ignored). Architecture agreed 2026-05-22.
> **Repo: https://github.com/wajahatkarim3/shipkit-site (private). Local path: ../shipkit-site.**
> **Day 1 done 2026-06-04** — Next.js 14 + Tailwind + TypeScript scaffold + landing page skeleton landed (commit `2a6006e`), then iterated through 12 follow-up commits same day: Navy + Gold brand repaint → UI UX Pro Max v2 deep-ink (ink-950 #020617) + Bento Features grid → gold-scarcity rule + one accent word per h2 → Pricing 5 side cards → PlatformStrip tooltips → FAQ card style → GitHub OAuth standard button → **full vibe-coder copy rewrite** (new headline "Ship your Android app. Without Android Studio.", strip SHA-1/Compose/OAuth jargon) → hero/header final (big video card back, real `shipkaro-mark.png`, "by shipkaro" attribution chip). Latest commit `73c49ae`. Build green throughout, ~87 kB First Load JS.
> Locked decisions (2026-06-04): LemonSqueezy MoR; sub-domain `kit.shipkaro.dev`; pricing $29 early-bird first 30 → $49; cohort path = 100%-off LemonSqueezy coupon; 14-day money-back; WhatsApp community only (no Discord yet); video hero (autoplay screen recording); generic "other kits" comparison (no named competitors); brand name = ShipKit; multi-platform teaser (Android Available + iOS/Flutter/React Native/Web "Coming soon" pills); **palette = Midnight Sky Navy `#00296B` + Gold `#FDC500` + Deep-ink `#020617`** (NOT the Android kit's purple — site and app no longer share a color); **typography = Geist Sans + Geist Mono**; **positioning = 3 pillars** (AI-agent ready / end-to-end / no Android Studio needed); **hero headline = "Ship your Android app. Without Android Studio."**; **TrustStrip = author credentials** (indie Android dev since 2014, 12+ apps, 300+ community); **CTA = standard GitHub-style black button** (not gold pill).
- [x] One Next.js site — public landing page scaffolded, design-locked (Navy + Gold + Deep-ink), copy-locked (vibe-coder rewrite), 12 commits pushed to `shipkit-site`.
- [x] **Docs site = plain MDX in app-router, NOT Nextra (built 2026-06-08)** — `kit.shipkaro.dev/docs`. Nextra dropped (its conventions fight the custom buyer-gating). Content lives in `shipkit-site/content/docs/*.mdx` (8 pages ported from the kit's `docs/`), rendered via `app/docs/[[...slug]]/page.tsx` (`next-mdx-remote/rsc` + `remark-gfm` for tables + `rehype-slug`/`autolink-headings`). `lib/docs.ts` (frontmatter parse + section-grouped nav), `app/docs/layout.tsx` (sticky sidebar shell, near-black `#0A0A0A` + phosphor `#A7F432`), `DocsSidebar.tsx` (active-link client), `MdxComponents.tsx` (styled element map). Build green, 8 docs prerendered SSG. Header + footer `/docs` links now resolve (were 404). `/changelog` still 404 (separate).
- [ ] GitHub OAuth (Auth.js) — auth-first purchase flow: GitHub sign-in → checkout → webhook adds buyer as private-repo collaborator. **Day 3 work.** (Docs-gating hook `lib/access.ts` `isBuyer()` already stubbed → returns false; wire Auth.js + repo-collaborator check here.)
- [~] Per-page docs gating — **mechanism built 2026-06-08**, `gated: true` frontmatter flag honoured: gated page + `!isBuyer()` → `PurchaseWall` component + `robots noindex`; public pages indexed. All current docs ship `gated: false` (nothing locked yet). Gate reads `lib/access.ts isBuyer()` — stubbed `false` until Auth.js lands. Sidebar shows 🔒 on gated items.
- [ ] Payment = Merchant-of-Record. **LOCKED: LemonSqueezy (primary), Paddle (tax-free backup).** User will set up LemonSqueezy product + API key. **Day 4 work — auto repo-add webhook integration.** Seller entity = **RemoteKaro LLC** (US, Wyoming) — used for LS KYB/KYC + all legal pages. Provider rationale (locked 2026-06-07): LS/Paddle are Merchant-of-Record → they collect+remit global sales-tax/VAT, seller owes zero tax compliance. **Stripe rejected as default** (ShipFast uses it, but Stripe is NOT MoR → RemoteKaro LLC would owe worldwide VAT/sales-tax itself; EU VAT from first sale, no threshold). If LS rejects → wire **Paddle** (also MoR, stays tax-free), NOT Stripe. The paid-webhook→add-GitHub-collaborator logic is provider-agnostic, so swapping providers later is a small webhook change. **Caveat:** legal pages name LemonSqueezy as MoR — a Stripe switch would require rewriting the "payments handled by reseller/MoR" sections (Paddle swap = just rename).
- [x] Pricing tiers + cohort-attendee free-access path — locked as above. Landing page Pricing component already encodes $29 → $49 + cohort callout + 14-day refund + multi-app license.
- [ ] LICENSE + per-buyer watermark; release tagging + changelog/migration-guide process. **Phase 8 / launch-prep work.**
- [ ] Hero video — user records speed-up of `/kit-start-setup` running, drops at `public/hero.mp4`. **User work.**
- [ ] Real screenshots (catalog light+dark, `/kit-start-setup` recording, auth flow, privacy policy preview). **User work.**
- [ ] OG image, sitemap, robots.txt, Vercel deploy + DNS for `kit.shipkaro.dev`. **Day 5 work.**

### Phase 8: Documentation
> **Docs written 2026-06-08** — full buyer-facing doc set in `docs/` (kit repo), modelled on swiftstarterkits.com/documentation IA (Getting Started → Components → reference), adapted to NowKit's **command-driven** reality. 8 markdown pages (~1150 lines): `README.md` (intro + docs map + two-layer-config model), `getting-started.md` (requirements + env-check + clone + first run + project structure + troubleshooting), `the-flow.md` (★ 0-to-Google-Play, 3 phases, every `/kit-*` command in order, first-version + update upload paths), `commands.md` (all 20 commands grouped + cheat-sheet + skills), `example-recipes.md` (explainer for the 5 recipes), `example-app-recipes.md` (the detailed recipe tables, pre-existing), `components.md` (40+ Kit components by category), `features.md` (auth/paywall/analytics/AI/ops/security internals). All cross-linked. **Ported to `shipkit-site/content/docs/*.mdx` + rendered at `kit.shipkaro.dev/docs`** (see Phase 7 docs-site entry). Pending: real screenshots (user work), deployment/troubleshooting deep-dive.
- [x] Getting Started (requirements, macOS/Windows setup, first run) — `docs/getting-started.md` (kit repo) + site `getting-started.mdx` (deduped → clone/first-run only)
- [x] **Environment Setup guide (2026-06-08)** — full per-OS (macOS/Windows/Linux) install walkthrough at site `environment-setup.mdx`, adapted from `shipkaro.dev/mobile-docs/setup-{macos,windows}`, KDoctor→`/kit-env-check`. **Site-only (not yet in kit repo `docs/`).**
- [x] Architecture overview — covered in `docs/getting-started.md` (project structure + default flow) + `docs/features.md` (two-layer config + per-system internals)
- [~] Component catalog docs (Swift-catalog parity: name + usage) — `docs/components.md` lists all 40+ by category; **screenshots still pending** (user provides; in-app Components Catalog screen covers live previews meanwhile)
- [x] Guides (adding a screen, toggling modules, rename package) — covered by command docs (`/kit-design-app`, `/kit-change-app-id`) in `docs/commands.md` + `docs/the-flow.md`
- [x] **Example app recipes (added 2026-06-07)** — `docs/example-app-recipes.md`. 5 buyer-facing end-to-end walkthroughs (`/kit-start-setup` → Play), each a different path: **A** Water Tracker (free/no-auth/offline/Room), **B** Habit Tracker (free + Supabase Google-only + questionnaire onboarding + OpenRouter AI + review dialog), **C** Calm Meditate (subscription + HARD paywall + Firebase Email+Google + Firebase analytics/remote-config), **D** QuickNotes (subscription + SOFT paywall + all-Supabase + Sentry + maintenance/update gate), **E** Everything-on tour (exploring + all 4 analytics + custom icon pack + RTL translate + catalog). Per-recipe choice tables + "what you'll see on device" + a sanity checklist. Doubles as the maintainer's manual device-test matrix (orthogonal — every branch covered once).
- [x] Permissions priming components (built 2026-05-22) — `KitPermission` enum + `rememberKitPermissionState` helper + `KitPermissionPrimer`/`KitPermissionPrimerSheet` components (components+helper only, not force-wired)
- [~] Deployment + Troubleshooting — `docs/getting-started.md` has a first-run troubleshooting table; `docs/the-flow.md` covers the deploy path (`/kit-upload-on-google-play`). Standalone troubleshooting/deployment deep-dive page still pending.

### Phase 9: Interactive Setup Commands (vibe-coder UX)  ✅ DONE 2026-05-30 (compileDebugKotlin green)
All slash commands live in `.claude/commands/` and are kit- prefixed so the dev can type `/kit-` to discover everything. Skills under `.claude/skills/`. Verbatim `>` blocks = present-as-is to the developer; prose = instructions to Claude.
- [x] `/kit-start-setup` — master orchestrator. 8 steps: orientation → rename → onboarding content (writes en strings) → theme (brand color only) → auth → paywall (hard-skip on free-app) → analytics → ops (remote config + push) → build. Each sub-command runs in-line; verify deferred to one build at the end.
- [x] `/kit-change-app-id` — wraps the `refactorPackage` Gradle task; also rewrites `/kit-run-app.md`'s baked applicationId.
- [x] `/kit-setup-theme` — brand color via `BrandPrimary` in `Color.kt`. Icon moved to release.
- [x] `/kit-setup-auth` — Stub / Supabase / Firebase + sign-in method toggles (Email-only / Google-only / Both via new `EMAIL_SIGN_IN_ENABLED` + `GOOGLE_SIGN_IN_ENABLED`). Per-provider Google sign-in: debug SHA-1 via `./gradlew signingReport`; release SHA-1 noted as release-time task. Supabase steps reflect current UI (Project ID under General; anon key under API Keys → Legacy anon, service_role tab).
- [x] `/kit-setup-firebase` — shared helper. Asks dev for the downloaded `google-services.json` path and Claude copies it into `app/`. Adds `google-services` + `firebase-crashlytics` plugins to catalog + root + app build.
- [x] `/kit-setup-paywall` — RevenueCat key + `PAYWALL_MODE` SOFT/HARD.
- [x] `/kit-setup-analytics` — PostHog (multi-select with Firebase Analytics + Crashlytics). PostHog steps updated to "Project token" under Settings → General.
- [x] `/kit-setup-updates` — remote config provider (LOCAL / **Supabase** / Firebase). Supabase path writes a SQL block for the `app_config` table. Push step gated. Deep links / App Links REMOVED — out of scope for micro-SaaS.
- [x] `/kit-compile-app`, `/kit-run-app` — runtime commands. Pre-baked with default applicationId; `/kit-change-app-id` rewrites `/kit-run-app` on rename. Step 9 of `/kit-start-setup` calls `/kit-run-app` for the one end-of-flow build.
- [x] `/kit-translate` — adds locales on demand. AskUserQuestion multi-select with 4 buckets: Keep English only / RTL (Arabic, Hebrew, Urdu) / Asia East-SE-South (Chinese S+T, Japanese, Korean, Hindi, Thai, Vietnamese, Indonesian, Malay, Filipino) / Europe (20 languages). Translates `values/strings.xml` → writes `values-<tag>/strings.xml`, updates `LocaleManager`, `locales_config.xml`, `localeFilters`.
- [x] `/kit-upload-on-google-play` — release flow. Branches first-version vs update. Fastlane removed entirely; manual Play Console upload. Release assets at `playstore/` (title, short_description, full_description, screenshots/, changelogs/). Screenshots step offers `aso-appstore-screenshots` skill OR user-provided. Listing copy step offers `aso-googleplay-listing` skill OR write-own. Data Safety form pointed at `legal/play-data-safety.md` template.
- [x] `/kit-design-app` — two-phase design + wire. Phase 1: pick source (Stitch MCP / screenshots / text / from scratch) → screen list → generate Compose files with KitTheme components + dummy data + `Route.<Name>` + `KitNavHost` wiring + strings to `values/strings.xml`. STOP for device approval. Phase 2 per-screen: data source (Supabase / Room / Retrofit / static) → repository + Koin binding + Supabase SQL migration → ViewModel → swap dummy data for `vm.state`. Never alter Phase 1 layout in Phase 2.
- [x] Skill: `aso-googleplay-listing` at `.claude/skills/` — Play-specific listing copy generator (no iOS subtitle/keywords fields). Writes the three `playstore/*_description.txt` files.
- [x] `/kit-setup-ai` (2026-06-01) — OpenRouter key, default-model picker (Fast & free / Cheap workhorse / Premium reasoning / pick own), flips `OPENROUTER_ENABLED`.
- [x] `/kit-setup-review-dialog` (2026-06-01) — wires `InAppReviewManager.requestReview()` at one of 4 trigger styles (Nth launch / key action / time delay / manual call-site). `reviewPrompted` DataStore flag prevents repeat fires.
- [x] `/kit-plan-release-analytics` (2026-06-01) — pre-release funnel planner. Auto-inserts `analytics.logEvent(...)` at Grep'd call-sites; ambiguous matches confirm with the dev. Wired into `/kit-upload-on-google-play`.
- [x] `/kit-generate-legal` (2026-06-01) — scans deps + KitConfig + Retrofit interfaces + Supabase tables; asks 8 legal Qs; writes `playstore/privacy_policy.md` + `.html` + filled `play_data_safety.md`. Wired into `/kit-upload-on-google-play`. ToS stays template-only.
- [ ] **Cross-tool command portability (added 2026-06-06)** — all 20 `/kit-*` commands + 2 skills are Claude-Code-native (`.claude/commands/`, `AskUserQuestion`/`TaskCreate`/`TaskUpdate`/`Skill` tool calls, cross-command inline `/kit-` orchestration, CLAUDE.md context, `>`-verbatim convention). They do NOT run as-is in **Cursor** or **Antigravity**. Port: (1) mirror commands → `.cursor/commands/*.md` (Cursor) + `workflows/*.md` (Antigravity); (2) add root `AGENTS.md` (both read it, neither reads CLAUDE.md) — symlink/generate from CLAUDE.md; (3) rewrite `AskUserQuestion`/`Task*` tool calls as plain prose ("Ask the user X, wait for answer"); (4) skills → Antigravity `.agent/skills/SKILL.md` (it has skills); Cursor has none → inline; (5) flatten `/kit-start-setup`'s inline sub-command calls (no cross-command invocation in Cursor/Antigravity); (6) **split files >12,000 chars for Antigravity workflow limit** — `kit-setup-auth.md` (14.6K), `kit-upload-on-google-play.md` (16.9K) over limit. Test each in both IDEs after porting.

### Phase 10: Default app flow (no demo)  ✅ DONE 2026-05-23 → revised 2026-05-30
Replace the old `Welcome → Demo` placeholder with a real default flow that USES every feature the dev configured, then nuke the demo entirely after runtime testing showed the demo broke the dev's no-auth/no-paywall choices.
- [x] Built default flow: `Splash` (1.5s) → `Onboarding` (first launch, DataStore-gated) → `Auth` (if `AUTH_ENABLED` + signed out) → `Paywall` (if `PAYWALL_ENABLED` + not premium + first time, DataStore `paywallSeen`) → `Home` → `Settings` / `Profile` / `Changelog`. Sign-out from any post-auth screen routes back to Auth.
- [x] New screens: `SplashScreen`, `HomeScreen` (now shows brand mark + heading + **System status** panel surfacing auth/paywall/analytics/remote-config state — replaces the old "Launch demo" button), `ProfileScreen` (read-only + sign out).
- [x] `CustomTabs` util — opens `KitConfig.PRIVACY_URL` / `KitConfig.TERMS_URL` via Chrome Custom Tabs.
- [x] `KitConfig` additions: `PRIVACY_URL`, `TERMS_URL`, `EMAIL_SIGN_IN_ENABLED`, `GOOGLE_SIGN_IN_ENABLED`. Removed `SAMPLE_FEATURE_ENABLED`, `dynamicColor` parameter.
- [x] `Route` adds Splash / Onboarding / Auth / Paywall / Home / Profile / Settings / Changelog; `Welcome` and `Demo` removed.
- [x] Settings: Account section (sign out + delete) gated by `AUTH_ENABLED`; Language row auto-hides when `LocaleManager.supported.size <= 1`; legal links wired to `KitConfig.PRIVACY_URL` / `TERMS_URL` via Custom Tabs.
- [x] **Demo NUKED 2026-05-30** — `feature/demo/` + `DemoNavHost` + `DemoDatabase` + `HabitRepository` + habit screens + `Route.Demo` + `demoModule` + `SAMPLE_FEATURE_ENABLED` + habit_* strings + `/kit-remove-demo` command + `kit-start-setup` "Make it yours" step. Reason: runtime testing with a non-coder revealed that "Launch demo" jumped into a sealed second app with hardcoded auth, contradicting the dev's no-auth choice. The kit's real flow now showcases everything end-to-end without a parallel demo.
- [x] Fastlane NUKED 2026-05-30 — `fastlane/` dir + `Gemfile` + Fastlane lines from `.gitignore`. `/kit-upload-on-google-play` uses manual Play Console upload; assets land in `playstore/`.
- [x] Urdu strings NUKED 2026-05-30 — `values-ur/` deleted; `localeFilters` / `locales_config.xml` / `LocaleManager.supported` reset to English only. `/kit-translate` adds languages on demand.
- [x] `.debug` `applicationIdSuffix` REMOVED — was causing Firebase `google-services.json` package-name mismatch for debug builds.
- [x] `local.properties.template` — OS-specific SDK path guidance (mac / Windows forward-slash advice / Linux) + per-key block (Supabase, RevenueCat, PostHog).
- [x] Supabase as `RemoteAppConfig` provider — `SupabaseRemoteAppConfig` reads `app_config` Postgres table; `supabaseClientModule` extracted in DI so the same `SupabaseClient` serves auth and remote-config (Postgrest plugin installed alongside Auth).

## Session Log
<!-- Update at END of each session -->
- **2026-06-11 (later — ASO + landing commands, recipes split to per-page, 0-to-Play SVG)**: Continued docs page-by-page (Reference → Recipes) AND added two release commands the flow was missing. **Spans both repos. NOT pushed at session start; pushed at end.** Work: (1) **Checked for an ASO command** — found the existing **`aso-googleplay-listing` skill** (already wired into `/kit-upload-on-google-play` Step F) does title/short/long + keyword strategy → `playstore/{title,short_description,full_description}.txt`. Gaps: not standalone; keyword confirm was "show + regenerate" not grouped. **Fixes:** new standalone **`/kit-generate-aso`** command (keep/regenerate/tweak-one branch wrapping the skill); skill Step 2 now **clusters keywords into 2–4 named groups + confirms via AskUserQuestion multiSelect** which to target. (2) **New `/kit-generate-landing`** — generates a **static HTML+CSS** marketing site (no framework) into a new **`landing/`** dir: `index.html` (hero + features + screenshots + Play badge) · `privacy.html` (reuses `/kit-generate-legal` output) · `terms.html` (fills `legal/terms-template.md`) · `styles.css` (uses `BrandPrimary` accent) · `assets/`. Contact = **email + social** (asks dev; no form/backend). Solves "where do I host the Play-required privacy URL" (`…/privacy.html`). Reference style = habitkit.app (calm, single-column). (3) **Wired into `/kit-upload-on-google-play`:** Step F runs `/kit-generate-aso` inline; Step G retitled "Data Safety, Privacy policy + Landing page" → after legal, **offers `/kit-generate-landing`** as the privacy-hosting path; task labels updated. Both added to `/kit-start-setup` outro optional list. (4) **`/kit-design-app` 1A enriched** — added **Figma URL** source (Figma MCP/Dev-Mode if connected, else export-frames-as-PNG path; Community-file duplicate note) + **Reference URL** source (Dribbble/store-page style guide) + "call out the UI patterns you see, confirm before building". (5) **Recipes split into separate pages** (site) — was `recipes.mdx` (index) + one big `recipes-detail.mdx`; now `recipes.mdx` = index (recipe table + how-to-use + device checklist + maintainer note) and **5 per-recipe pages** `recipe-{water-tracker,habit-tracker,calm-meditate,quicknotes,everything}.mdx` (orders 2–6); deleted `recipes-detail.mdx`. Each recipe ship-steps gained `/kit-generate-aso` + `/kit-generate-landing`. **Design references** (real PNGs in `public/docs/recipes/`): Water Tracker → [Dribbble water shot] + img (design-app **Reference URL** source); Habit Tracker → HabitKit streak-grid img (**Screenshots** source); QuickNotes → [Figma Community Notes UI] URL + hero img (**Figma URL** source). Calm Meditate + Everything kept same (no design ref) but still split + new commands. (6) **Recipes index table** — user: "Best when your app…" column wraps weird + titles wrap 2 lines; dropped that column + redundant App-name column → `Recipe · Pricing · Login` (single-line titles). (7) **0-to-Google-Play diagram** — user supplied hand-built `nowkit-0-to-play.svg`; replaced the ASCII header flow in `the-flow.mdx` with it (`public/docs/nowkit-0-to-play.svg`); kept bottom command cheat-list (now lists aso/landing). **Docs updated for both new commands** in `commands.mdx` (Run+release + skills note + cheat-sheet), `the-flow.mdx` (new Steps 3.2 ASO + 3.3 Landing, renumbered, table F/G), `launch-roadmap.mdx`. **All site builds green (44 static pages).** **Next: continue docs page-by-page** (Components / Screens / Backend detail pages); pending unchanged — Auth.js + `isBuyer()` gating, LS checkout-link + repo webhook (LS in review), kit device-test, 20 doc screenshots (user).
- **2026-06-11 (docs page-by-page cont. + kit improvements surfaced by user device-testing)**: Continued the page-by-page docs polish AND fixed several kit gaps the user hit while running the kit on a real "QuickNotes" app. Spans both repos. **Pushed in ~5 commits across the session.** Work: (1) **`/kit-save-to-github` (new command)** — closes the "docs say clone the kit but nothing helps a non-coder push their *new app* to GitHub" gap. Agent-driven via `gh` CLI: Step 0 detects whether `origin` is still the kit repo (first-time) vs the user's repo (re-save); ensures `gh` installed + `gh auth login`; first-time = safety-check `local.properties` is gitignored → detach kit origin → commit → `gh repo create <name> --private --source=. --push`; re-save = add/commit/push. Decisions: **Both** triggers (standalone + offered at end of setup) + **gh CLI** method (user picked via AskUserQuestion). Wired into `kit-start-setup` (offer) + `kit-env-check` (gh as optional tool: probe + install block) + docs `creating-new-project` §1 callout. (2) **creating-new-project restructure** (user feedback: numbered steps imply "do step 3,4,5" but §3-5 are reference, + a dir tree scares a terminal-only vibe coder): **numbered = actions only** (1 Project setup, 2 First run); everything else **unnumbered reference** (How your app flows / The app's structure [softened "you don't need this to start" intro, moved lower] / Two kinds of config / Kit updates). Added the **real NowKit app-flow diagram image** (`public/docs/app-flow.png`, 2.7M — Splash→Onboarding→Auth→Paywall→Home→Settings phone shots) to "How your app flows". §1 simplified by user (no upstream git). (3) **Command-block styling** (`MdxComponents.tsx`) — user: "literal commands too small". Bare single-line `/kit-*` fences now render as a **phosphor terminal pill** (`›` prompt + large bold `#A7F432`); other code blocks bumped 13px→15px. (4) **Paywall billing — big pass** (user mid-`/kit-setup-paywall`, sub-step 2.6 was a wall of text): new **`/docs/paywall`** page (Reference, kit-perspective + AI-first: how the paywall works, what setup does, the release-time billing steps, testing, troubleshooting); **`com.android.vending.BILLING` permission** added to AndroidManifest **commented + paywall-gated** (`/kit-setup-paywall` uncomments on enable, free app keeps it commented — verified via RevenueCat docs: needed to unlock Play product creation); **2.6 slimmed to now/later** AskUserQuestion (later = 3-bullet summary + docs link; now = paced walkthrough); **Step 0 menu** gains "Set up products + Play billing" re-entry (since 2.6 is dashboard-side, kit can't detect it). (5) **Billing lead-time → start early** (user: "what takes 36h+ time?"): identified the two clocks startable after first run — RevenueCat↔Play service-account JSON (**~36h propagate**) + a build on an **internal testing track** (first review days; products only load for a build on a track + tester opt-in). New **`kit-start-setup` Step 8** (paywall-only): after successful run, AskUserQuestion to start both now (run 2.6-now + upload to internal testing, keep building while clocks tick) or later. `kit-upload-on-google-play` **A.1** rechecks billing readiness (BILLING perm + Verified propagation + products + offering + track/opt-in). User **rejected** a proposed `DEBUG_FORCE_PREMIUM` toggle ("bad workaround, vibe coders miss it before launch"). (6) **Icons ship Material-only by default** (user: "picking Material still comments feather/tabler in gradle — why aren't they off by default?"): commented `composeicons-feather/tabler` in `libs.versions.toml` + `build.gradle.kts`, wrapped `FeatherKitIcons.kt`/`TablerKitIcons.kt` bodies in `/* */` (Kotlin nested-comment safe; `compileDebugKotlin` green). `/kit-setup-theme` logic flipped: Material pick = no-op; Feather/Tabler pick = **enable** (uncomment dep + unwrap impl + set binding); old "Apply/disable" reframed as disable-on-switch. (7) **`kit-start-setup` wrap-up de-cluttered** (user: "huge scroll, info overflow"): GitHub backup → **AskUserQuestion** (Yes create private repo / Not now, benefits in option descriptions) instead of a paragraph callout; the giant optional-commands verbatim block (multi-line descriptions) → **compact one-line-each list**. **Builds green throughout** (kit `compileDebugKotlin`; site 39→ pages). **Next: continue docs page-by-page** (The Journey / Reference deep pages / Components / Screens / Backend / Recipes); pending unchanged — Auth.js + `isBuyer()` gating, LS checkout-link + repo webhook (LS in review), device-test rest, 20 doc screenshots (user).
- **2026-06-10 (docs-site UX + Getting Started restructure — all in `shipkit-site`, page-by-page with user)**: Page-by-page docs polish pass (user driving, "we go page by page"). **No kit code touched — site only. NOT pushed yet at session start; pushed at end.** Work: (1) **Mobile nav** — docs sidebar was `hidden lg:block` (no nav on phones) + landing header had no hamburger. Added mobile hamburger to shared `CleanHeader` (`md:hidden`, expands full-width panel w/ Features/Stack/Pricing/FAQ/Docs + Sign in) and new `components/docs/MobileDocsNav.tsx` (collapsible "Docs / <current page>" bar below header, `lg:hidden`, expands full section/page tree, auto-closes on route change), wired into `app/docs/layout.tsx`. (2) **Sidebar section-vs-page distinction** (`DocsSidebar.tsx`) — sections now read as group headers (wider tracking, hover bg when collapsed); pages nest under a `border-l` tree rail w/ indent + active-page phosphor tick. (3) **Introduction page** — was `## Introduction` duplicating the frontmatter H1 title; restructured SSK-style (modelled on swiftstarterkits.com/documentation/introduction): intro prose → Why a starter kit? → What you get → How docs organised (moved to end) → Where to go next. Then **made Introduction a top-level ungrouped page** (no collapsing header) above the GETTING STARTED group: `lib/docs.ts` treats empty-string `section` as top-level sorted first; `DocsSidebar` renders empty-section items as plain links. (4) **Getting Started section rebuilt to match the old KMM kit IA** (Requirements / Setup macOS / Setup Windows / First Run): new **`requirements.mdx`** (hardware + required accounts GitHub + an **AI-agent** section [user wants all 4 agents — Claude Code / Cursor / Antigravity / OpenCode — treated as equally supported, NO "coming soon"; explains kit is AI-first + you can still build by hand without an agent] + optional-accounts table mapping Supabase/Firebase/Google Cloud/RevenueCat/PostHog/Sentry/OpenRouter → their `/kit-setup-*` command + cost + Play Developer $25/14-day-tester note); **split `environment-setup.mdx` → `setup-macos.mdx` + `setup-windows.mdx`** (deleted env-setup; macOS page carries a short Linux appendix; step 5 Node.js relabelled "runs the CLI agents (Claude Code, OpenCode)" since Cursor/Antigravity are standalone apps; step 6 "An AI agent" lists all 4 w/ install methods, no Claude-only framing; added the **Android Studio → ⋮ → SDK Manager** screenshot to §3, and split the old "scrcpy + Holo" step into two standalone sections w/ 1-2-line explainers + GitHub links [scrcpy=Genymobile/scrcpy, Holo=measure-sh/holo] + screenshots). Real PNGs saved under `public/docs/setup/` (android-studio-sdk-manager 1.4M, scrcpy 222K, holo 2.9M). (5) **`getting-started.mdx` → renamed `creating-new-project.mdx`** (`git mv`, slug `creating-new-project`, title "Creating a New Project", order 4) so the URL matches the title; added the **two-layer config model** here (moved out of Introduction); **§1 Project setup simplified by the user** (dropped the `upstream`-remote git flow — too scary for vibe coders — now just clone into a folder + open in agent); **§5 rewritten** from a git fetch/merge/upstream walkthrough to plain-English **"Kit updates"**: kit = a starting point you *copy*, once you build inside a clone it's your app (no pulling future kit changes — keeps it simple + can't break a shipped app), lifetime updates = start your *next* app from a fresh copy of the latest kit. Rewrote all cross-links (`/docs/environment-setup` → setup-macos/windows; `/docs/getting-started` → `/docs/creating-new-project`) across requirements/setup-macos/setup-windows/index/launch-roadmap. `lib/docs.ts` derives slug from **filename**, not frontmatter (frontmatter `slug` is decorative). **All builds green throughout (39 static pages).** Sidebar now: `Introduction` (top-level) / **GETTING STARTED** → Requirements · Setup macOS · Setup Windows · Creating a New Project. **Next (tomorrow): continue docs page-by-page** (The Journey / Reference / Components / Screens / Backend / Recipes); still pending from before — Auth.js + `isBuyer()` gating, LS checkout-link + repo-add webhook (LS in review), kit device-test, 20 doc screenshots (user).
- **2026-06-09 (cohort-2 launch prep — roadmap, tracker app, launch playbook; spans 3 repos)**: Cohort 2 sold out (**27 attendees**), starts ~2026-06-12. User wants students to actually launch this time (last cohort: nobody shipped). **Diagnosed the real blockers:** (1) **Google Play's closed-testing rule** — new *personal* Play accounts must run a closed test with **12+ testers opted in for 14 continuous days** before applying for production → "launch on the weekend" was never possible; launch is ~3 weeks out. (2) **No accountability** after Day 2. **Unlock = the cohort tests each other** (shared Google Group as testers → instant 12+). **Built 3 artifacts:** (a) **Launch Roadmap** docs page — `shipkit-site/content/docs/launch-roadmap.mdx` (The Journey, order 2, `gated:false` for now → flip when Auth.js lands), the realistic 3-week build→closed-test→14-days→production→live timeline mapped to `/kit-*` commands; site commit `a9373d0`. (b) **Launch to-do** (P0/P1/P2 critical path to first sale) + **cohort tracker template** in `.session/` (git-ignored). (c) **Cohort 2 Apps Tracker web app** in the **`../ShipWithClaude`** repo (separate; deploys to `1dayapp.shipkaro.dev/cohort2`) — a shared live tracker so the user watches all 27 students across 11 milestones (Kit Setup → Live on Play). Stack reuses ShipWithClaude's existing **Supabase + Vercel serverless**: new `cohort2_progress` table (`supabase-cohort2-schema.sql`), `lib/cohort2.js`, `api/cohort2/{login,list,update}.js`, `cohort2.html` (status matrix = whole-cohort screenshot; per-student drawer = individual). **Auth = email-only**: allowlist = the existing `purchases` table (= all 27 attendees, no rename so hero avatars/seat-count untouched) + `ORGANIZER_EMAILS` env var so the organizer (not a buyer) gets in without polluting `purchases`. Branded "1 Day App by ShipKaro" header + a **tester-pool banner** sharing `shipkaro-cohort2@googlegroups.com`. ShipWithClaude commits `5797f45` (tracker) → `9115b2d` (organizer access) → `d9d49f0` (branding+banner), pushed. **User TODO:** ran the schema ✅; set `ORGANIZER_EMAILS` in Vercel + redeploy; create the Google Group + have all 27 join it. **This was cohort/business work, not kit code — kit + docs status unchanged from the 2026-06-09 entries below.**
- **2026-06-09 (docs site UI polish — header unification, collapsible sidebar, looping logo)**: Late-night `shipkit-site` UI fixes after the docs build-out. (1) **Shared header** `6ba4276` — docs had its own stripped header (tight `/nowkit DOCS` wordmark, only Features/Pricing/Get-NowKit, no Sign-in) vs the landing's `CleanHeader` (MetallicMark + Features/Stack/Pricing/FAQ/Docs + Sign in + Get the kit). User wanted ONE header everywhere → docs `layout.tsx` now renders `CleanHeader`; **made `CleanHeader` path-aware** (`"use client"` + `usePathname`): on `/` hash links scroll in place (smooth-scroll script intact), off-home they prefix `/` (`/#features`) so they navigate home first; logo → `/`. Sidebar offset bumped to `h-16` header. (2) **Collapsible sidebar + visible scrollbar** `7dfafe0` — `DocsSidebar` sections now collapse (chevron + smooth grid-rows expand); only the section holding the current page starts open, auto-opens the active section on navigation, user toggles the rest → cuts the long scroll. Added `.docs-scroll` thin scrollbar in `globals.css` (subtle white, phosphor on hover; webkit + Firefox). (3) **Looping logo** `328963c` + user timing tweak — `MetallicMark` was one-shot type-on-mount; now loops type → hold → erase → pause → retype forever (reduced-motion still static). User tuned timing to `HOLD_FULL_MS=8400` / `HOLD_EMPTY_MS=250` (long hold, quick retype). Applies to every mark instance (landing + docs header + footer). All builds green, 36 static pages. **Earlier same-session (2026-06-08 cont.) green-overload fix, favicon, env-setup page, getting-started dedup, index map refresh — see 2026-06-08 entries.** **Next unchanged:** Auth.js+gating activation, LS checkout-link+webhook (LS in review), kit device-test, 20 doc screenshots (user) → flip `IMAGES_READY`.
- **2026-06-08 (Phase 8 docs written + Phase 7 MDX docs site built)**: Confirmed LS price fix ($29.99 → $29, marketing now consistent — checked off). Reviewed remaining kit work (code feature-complete; left = device-test + docs). **Wrote the full buyer-facing doc set** in the kit repo `docs/`, modelled on **swiftstarterkits.com/documentation** (fetched its IA: Getting Started → Components → Version History) but adapted to NowKit's **command-driven** reality — the `/kit-*` commands ARE the product, so docs are organised around them. 8 pages, ~1150 lines, all cross-linked: `README.md` (intro, docs map, two-layer KitConfig/RemoteAppConfig model), `getting-started.md` (requirements, `/kit-env-check`, clone, first run, package tree, default flow, troubleshooting table), **`the-flow.md`** (★ the centerpiece the user asked for — 0-to-Google-Play as 3 phases Set-up/Build/Ship, ASCII flow diagram, every command in order, first-version A–K + update upload paths), **`commands.md`** (all 20 commands grouped Start/Setup/Build/Run-Release + cheat-sheet + the 3 skills), **`example-recipes.md`** (explains what recipes are + per-recipe teaching, links the detailed `example-app-recipes.md`), `components.md` (40+ Kit components by category + foundation + 8 permission flows + utils), `features.md` (auth/paywall/analytics/AI/ops/security internals + config-model table). **Then scaffolded the docs SITE** in `../shipkit-site` — user OK'd **plain MDX in app-router, dropped Nextra** (Nextra's conventions fight the custom buyer-gating; we hand-rolled everything else anyway). Installed `next-mdx-remote` + `gray-matter` + `remark-gfm` (GFM tables — initially forgot, tables rendered blank until added) + `rehype-slug` + `rehype-autolink-headings`. Ported the 8 docs → `content/docs/*.mdx` with frontmatter (title/slug/section/order/gated), rewrote all `.md` cross-links → `/docs/*` routes. Built: `lib/docs.ts` (frontmatter parse + section-ordered nav: Getting Started/The Journey/Reference/Recipes), `lib/access.ts` (`isBuyer()` gating stub → returns false until Auth.js), `app/docs/[[...slug]]/page.tsx` (RSC MDXRemote + `generateStaticParams` + `noindex` on gated + `PurchaseWall` for gated+non-buyer), `app/docs/layout.tsx` (sticky sidebar shell, near-black `#0A0A0A` + grid + phosphor `#A7F432`, matches LegalShell), `components/docs/DocsSidebar.tsx` (client active-link + 🔒 on gated), `MdxComponents.tsx` (full styled element map — headings w/ scroll-mt anchors, phosphor links, code, tables, blockquote callouts). **Build green, 8 docs prerendered SSG.** Header + footer `/docs` links (were 404) now resolve. **Gating mechanism is BUILT but dormant** — all docs ship `gated:false`, `isBuyer()` stubbed false; wire Auth.js + GitHub repo-collaborator check into `lib/access.ts` to activate. **Committed + pushed both repos:** kit `086b453` (docs/ + CLAUDE.md), site `b8a2ffa` (MDX docs scaffold). **Then fixed the last `/changelog` 404** — new `app/changelog/page.tsx` renders `content/changelog.mdx` (seeded v1.0.0 launch entry) reusing the docs `mdxComponents` + LegalShell-style dark shell; footer nav now fully resolves (only WhatsApp `#` links remain placeholder). Site `27fbf13`, pushed. Build green, 22 static pages. **Next: Auth.js + GitHub repo-collaborator check → wire `lib/access.ts isBuyer()` (activates docs gating); LS checkout-link wiring + repo-add webhook (gated on LS approval); kit device-test; real doc screenshots (user work).**
- **2026-06-08 (cont. — detailed SwiftStarterKits-style component/screen doc pages + image manifest)**: User wanted per-component/per-screen detail pages like swiftstarterkits.com (fetched its anatomy: title → 1-2 line desc → 2 device shots light+dark → Usage code → Features bullets → Notes). Built **11 new MDX pages** in `shipkit-site/content/docs/` with **real Kotlin APIs extracted from source** (grepped actual composable signatures + manager methods — no invented APIs): 4 component-category pages (`components-buttons` / `-layout` / `-feedback` / `-navigation`, section "Components"), 6 screen pages (`screen-auth` / `-onboarding` / `-paywall` / `-settings` / `-permissions` / `-ops`, section "Screens"), 1 `managers` page (AuthRepository/PurchaseManager/AnalyticsManager/OpenRouterAiRepository/UpdateManager/InAppReviewManager/SecureDataStore, section "Backend"). `lib/docs.ts SECTION_ORDER` extended (Components/Screens/Backend slot between Reference and Recipes); `components.mdx` moved into Components section as the index w/ links to the 4 detail pages. **Screenshot strategy:** new `components/docs/Shot.tsx` (+`Shots` 2-up wrapper) renders **labelled phone-frame placeholders** showing each image's exact target path (so pages look intentional NOW + double as an inline image manifest) — flip one const `IMAGES_READY=false→true` when PNGs land, no per-page edits. Registered `Shot`/`Shots`/styled `img` in `MdxComponents.tsx`. **20 images needed**, dirs created `public/docs/{components,screens}/` + full manifest at `public/docs/IMAGES.md` (specs: PNG, 9:19.5 portrait ~1080×2340, light+dark pairs, brand-themed): 8 component-category (4×light/dark, from in-app Components Catalog sections) + 12 screen (6×light/dark). Build green, **33 static pages** (was 22). Site `0b6fc3d`, pushed. **User will provide the 20 screenshots** → drop at the manifest paths → flip `IMAGES_READY`. **Then 3 polish passes (same day):** (1) **green-overload fix** `b8487fb` — user flagged docs "too much green / unreadable"; root cause = `rehype-autolink-headings behavior:wrap` wrapped every heading in an `<a>` so the green link style hit all headings (green+underlined); dropped autolink (kept `rehype-slug` for ids), inline `code` green→neutral white, links green-text→white-text-with-green-underline. Green now only at active-nav/callout-border/link-underline. (2) **favicon** `834fc2e` — favicon.io zip → `app/favicon.ico` + `app/apple-icon.png` (Next file-convention) + 16/32/android-chrome PNGs in `public/` + `site.webmanifest` (renamed NowKit + dark `#0A0A0A` theme, favicon.io shipped empty) + layout metadata `manifest`+icons. (3) **Environment Setup page** `980af8d` — per-OS (macOS/Windows/Linux) install walkthrough adapted from `shipkaro.dev/mobile-docs/getting-started/setup-{macos,windows}` (fetched both): JDK 17 → Android Studio SDK (API 35 + cmdline-tools) → ANDROID_HOME → Node → Claude Code → android CLI + `android skills add --all` → optional scrcpy/Holo. **KMP KDoctor replaced with `/kit-env-check`** (kit is pure-Android). New "Getting Started" doc, order 2 (Introduction 1 → Environment Setup 2 → Getting Started 3), cross-linked both ways. 36 static pages now. (4) **Environment Setup dedup + index refresh** `1e03b52` + `33466ad` — getting-started's duplicate Requirements section removed (now owned solely by Environment Setup; getting-started = clone→first-run→structure); `index.mdx` docs-map rewritten to mirror all 7 sidebar sections (Getting Started / The Journey / Reference / Components / Screens / Backend / Recipes) + features.mdx cross-links to its Screen + Managers counterparts. **Docs site canonical = `shipkit-site/content/docs/` (17 MDX pages).** **Kit repo `docs/*.md` (8 plain-md, the buyer-repo copy) is now a LIGHTER SUBSET — behind the site** (no Environment Setup page, no 11 detail pages, getting-started.md still has the old Requirements section). **Future task: sync kit `docs/` from the site, or keep it as a deliberate lighter pointer set.** **Next: same as before** — Auth.js+gating activation, LS checkout-link+webhook (LS still in review), kit device-test, 20 doc screenshots (user).
- **2026-06-07 (cont. — LemonSqueezy store created + product published, real-time walkthrough)**: Walked the user through the entire LS store setup live (they'd been **rejected twice before** — diagnosed via a screenshot of the rejection email: prior store was `remotekaro.com` with "roast my resume" + masterclass = **services**, which LS explicitly bans ["we must prove something was delivered at the time of purchase; services happen off our site"]). Key insight: those were **product-scope** rejections, NOT identity/country — NowKit (pure digital software, license-key + repo delivery) is dead-center in LS's approved category, so a clean software-only store passes. **Store built:** name `nowkit`, URL `nowkit.lemonsqueezy.com`, **country = United States** (RemoteKaro LLC + US bank — NOT Pakistan, which likely contributed to nothing since prior rejects were scope, but US is the supported-seller path). Identity verification via existing **RemoteKaro LLC Stripe entity** (LS uses Stripe Connect as its payout/KYC rail — LS still MoR; this is NOT the "Stripe as processor / owe global tax" path) → submitted, **"In Review."** **Email = `kit@shipkaro.dev`** (user already had this domain mailbox) — synced across all legal pages + the Getting-Started PDF (was `remotekarollc@gmail.com` / `hello@shipkaro.dev`). **Product published:** "nowkit by shipkaro - Android starter kit", **Single payment**, tax category **Software** (downloadable licensed software, NOT SaaS/service), software-framed description ("digital software product… instantly receive a license key… delivered digitally and automatically at checkout… one-time purchase; build unlimited apps"), **Generate license keys ON** (length + activation BOTH unlimited = lifetime, matches one-time purchase), cover image + Getting-Started PDF attached (= the "something delivered at purchase" LS requires), Website link `kit.shipkaro.dev`, confirmation modal + email receipt customized (license key + "reply with GitHub username for repo access" + PDF download button). **DANGER caught:** user almost connected `kit.shipkaro.dev` as the LS store's custom domain (would've pointed the A record at LS IP `3.33.255.208` and **overwritten the Vercel site**) — stopped them; LS-hosted checkout (`nowkit.lemonsqueezy.com`) + "Buy" buttons linking to it is the right architecture; the "website" field is just informational text. **Flagged open item (RESOLVED 2026-06-08):** LS price was **$29.99** but ALL marketing (landing, cover, PDF, early-bird copy) says **$29** → user set price to `29.00` ✅ (confirmed fixed 2026-06-08). **Assets generated this session (all in `shipkit-site`, rendered from real component specs via headless Chrome per the no-AI-raster rule):** `public/nowkit-avatar.png` (500×500 Google DP, prior session), `public/nowkit-logo.png` (512×512 `/n▌` square store/social logo), `public/nowkit-cover.png` (1600×1200 product cover w/ headline + `$ /kit-start-setup` + $29), `public/nowkit-getting-started.pdf` (223 KB branded delivery PDF). Generators kept in `shipkit-site/scripts/*.html`. `kit.shipkaro.dev` domain connected to Vercel (Valid Configuration / Production). **Status: product published, store activation gated on identity review (~2-3 business days) — waiting on LS email.** **Next (tomorrow):** confirm price = $29; once LS approves → grab the live checkout link (`nowkit.lemonsqueezy.com/checkout/...`), wire it into the site's "Buy"/CTA buttons, then build the repo-add webhook (paid → add buyer as private-repo collaborator) + Auth.js. Also still pending: kit device-test, Nextra `/docs`, stub the `/docs` + `/changelog` 404s.
- **2026-06-07 (legal pages + LemonSqueezy application prep + payment decision)**: User wants to apply to **LemonSqueezy now** to parallelize the ~2-3 business-day activation review (it's the long pole — LS reviews KYC/KYB + product legitimacy before you can sell). Verified via web search: digital software w/ license-key/repo-access fulfillment is an **approved category**; you can create the product + submit for review BEFORE the webhook/Auth.js exists (webhook configured post-approval w/ the API key). Blocker found: site footer linked `/privacy` + `/terms` + `/license` but **those pages didn't exist** (only root + 4 palette routes) → 404 = a reviewer red flag. Built the full legal set on `shipkit-site`: new `components/LegalShell.tsx` (dark-theme shell, self-contained header/footer — no broken home-anchor nav) + `.prose-legal` styles in globals + 5 routes **`/privacy` `/terms` `/refund` `/contact` `/license`**. Seller entity = **RemoteKaro LLC** (user has it; US **Wyoming**) — chose LLC over Wajahat-individual because US LLC = clean LS KYB + W-9 + US payouts + looks legit. Contact email `remotekarollc@gmail.com`. Updated `CleanFooter`: added Refunds + Contact links, © → RemoteKaro LLC. globals.css gotcha: my appended `.prose-legal @layer` block added a stray `}` (orchid rules were top-level, not in a layer) → "Unexpected }" build fail → fixed by removing stray brace + closing the new layer. Build green (5 new static routes ~186 B each). Pushed `df6852d` + Wyoming fill-in. **Payment-provider decision (user saw ShipFast uses Stripe + got GitHub access, asked re Stripe backup):** LOCKED **LemonSqueezy primary, Paddle backup, Stripe rejected as default** — LS/Paddle are Merchant-of-Record (collect+remit global VAT/sales-tax, seller owes zero); Stripe is NOT MoR so RemoteKaro LLC would owe worldwide tax itself (EU VAT from first sale, no threshold). If LS rejects → Paddle (tax-free), not Stripe. Webhook→repo-add logic is provider-agnostic so swapping is cheap; caveat = legal pages name LS as MoR (Stripe switch needs MoR-wording rewrite, Paddle = rename only). **Still 404 (non-blocking for LS review):** `/docs`, `/changelog`, WhatsApp `#` footer links. **Next: user submits LS application (create store + product $29 + RemoteKaro LLC KYB + W-9 + submit); meanwhile resume kit device-test + docs.**
- **2026-06-05 (BRAND RENAME ShipKit → NowKit)**: User flagged "ShipKit" is crowded on Google (Docker BuildKit, the old LaunchKit, multiple "ShipKit" starter kits). Wanted a verb+kit alternative, easy to pronounce, app/code/indie-hacking flavored. Brainstormed + web-scanned a long list — found the `[common-word]+Kit` namespace is **structurally mined out** in dev tooling: ShipKit, BuildKit (Docker/moby), LaunchKit (old mobile-dev tools), SoloKit (getsolokit.com + solo-io/solo-kit + SoloDevKit — worst, same AI-coding-for-Claude-Code audience), AppKit (Apple framework — dead on arrival), MobileKit (generic), FastKit (`@fastkit` npm + FastAPI gravity), SpeedKit (speedkit.com Baqend company). Only two survived a clean web scan: **Quickit** (cleanest collision profile, portmanteau, but pronunciation wobble quick-it/quick-kit) and **NowKit** (crisp pronunciation, "ship now" energy, only a faded Vercel-"Now" shadow). User picked **NowKit**. **Renamed across `../shipkit-site`** via two case-sensitive subs (`ShipKit`→`NowKit`, `shipkit`→`nowkit`) over 19 files: `app/layout.tsx` + 4 palette layouts (title/OG/Twitter/siteName metadata), `components/clean/MetallicMark.tsx` (`FULL = "/nowkit"`, aria-label, comments), `Header.tsx` (wordmark + "Get NowKit" CTA), `Hero.tsx`/`FinalCta.tsx`/`CleanFinalCta.tsx`/`CleanHero.tsx` ("Get NowKit — $29" CTAs), `Footer.tsx`, `Comparison.tsx`/`CleanComparison.tsx` (heading "Why NowKit?" + `shipkit` TS data field renamed to `nowkit` through interface/rows/`r.nowkit` access), `Faq.tsx`/`CleanFaq.tsx` (vibe-coder Q copy), `README.md`, `tailwind.config.ts` comment, `package.json` name → `nowkit-site`. **"by shipkaro" attribution chip + `shipkaro-mark.png` logo untouched** — parent brand stays. zsh gotcha: unquoted `$files` var doesn't word-split (unlike bash), so the first perl run failed "Can't open <all files>" — fixed by passing files inline. `npm run build` green (`/` 4.41 kB, 6 routes prerender). Committed `2b44638`, pushed to `shipkit-site` main. **500×500 Google-profile avatar** — user wanted `/nowkit` + green cursor DP, and explicitly wanted it built **from the website's real mark, not an AI raster** (I'd reached for `mcp__gemini__generate_image` — user stopped me). Read `MetallicMark.tsx` for the exact spec, wrote a static HTML (`scripts/nowkit-avatar.html`) embedding Geist-Variable @ weight 800 via `file://` @font-face, rendering `/nowkit▌` on `#0A0A0A` + faint brand grid + ambient phosphor blob, slash `#E5E5E5`, word chrome-gradient `#FFF→#6E6E6E` (`-webkit-background-clip:text`), cursor block `#A7F432` + glow. Rendered with headless Google Chrome (`--headless --screenshot --force-device-scale-factor=2 --window-size=500,500`) → 1000×1000, `sips -z 500 500` downscale → `public/nowkit-avatar.png` (+ `@2x`). No rsvg/cairosvg/puppeteer on machine; Chrome.app was the available renderer. **Deliberately NOT renamed** (kept `shipkit`/`kit`): GitHub repo `shipkit-site`, Vercel URL `shipkit-site.vercel.app`, all `/kit-*` slash commands (generic, not brand-bound), this Android kit's package/applicationId. **Rename follow-ups resolved 2026-06-06:** GitHub repo + Vercel project + URL keep `shipkit-site` on purpose (brand display name only changed); public domain = `kit.shipkaro.dev` (user wires it on Vercel); `nowkit.dev` grab + trademark/Play scan dropped. **Next: same Phase 7 pending items** — hero video, real screenshots, Nextra docs, Auth.js, LemonSqueezy, deploy.
- **2026-06-05 (Phase 7 site Day 2 — massive design pivot)**: Huge sweep. Threw out the Navy + Gold + Geist-sleek design, kept it archived at `/sleek`. Built a fresh **OSForms-inspired dark + grid + chrome-wordmark** theme at `/`. Promoted `/clean` (built earlier in the session as a layout experiment) to the root after multiple iterations + user feedback. Old root content lives at `/sleek/`. Three palette experiments live alongside (`/forest`, `/neon`, `/orchid`) as CSS-skin overrides on the shared sleek components — they survived the pivot for reference. **Design exploration arc:** before settling on the OSForms-inspired direction, ran 4 throw-away variants. (1) `/atelier` — Atelier-style editorial pastel + Fraunces serif italic + soft gradients — user hated it, nuked. (2) `/forest` — Hunter Green + Vanilla Cream + Yellow Green palette (color.bears Threads palette #1) — CSS-skin scope on shared components, no new components needed. (3) `/almond` — Velvet Curfew + Almond Hearth (light-mode pastel) — user hated, nuked. (4) `/neon` — Electric Sun + Hyper Neon + Obsidian Root — Linear/Vercel-loud, kept. (5) `/orchid` — Midnight + Royal Blue + Dusty Orchid (light-pink page + dark midnight cards) — required CSS-var inheritance trick to handle dual text-context (text inside dark slabs vs text on pink page) without compound-selector circular-dep errors in Tailwind. **OSForms structural clone** — looked at `osforms.com` (open-source forms backend) for the new main `/`. Their formula: dark near-black bg + subtle grid scaffold + chrome-embossed wordmark logo + pill CTAs + mixed-weight headlines (bold white + dim gray-gradient accent phrase) + colored brand-logo framework strip + 01/02/03 numbered steps + Bento feature grid + FAQ accordion + minimal footer. Built 12 components under `components/clean/`: `CleanHeader`, `CleanHero`, `CleanPlatformStrip`, `CleanTrustStrip`, `CleanFeatureCards`, `CleanShowDontTell`, `CleanTechStack`, `CleanComparison`, `CleanPricing`, `CleanFaq`, `CleanFinalCta`, `CleanFooter` + supporting `MetallicMark`, `SmoothScrollScript`, `TerminalTyper`. **Two big mistakes I had to clean up:** (a) first build used LIGHT theme (white bg) — OSForms is DARK. User flagged. Pivoted to `#0A0A0A` near-black + grid overlay. (b) first build invented new copy ("Simple 3-step setup" / "One kit. Infinite flows.") — should've kept ShipKit's original copy + applied OSForms aesthetic only. User flagged hard. Rewrote every component to use the existing ShipKit copy verbatim (hero h1 "Ship your Android app. Without Android Studio.", 6 features w/ original emojis, 4 ShowDontTell sections, 16-pill TechStack, 10-row Comparison, 13-line includes + 5 side cards, 11 FAQs, "Stop perfecting. Start shipping.", 4-col footer). **Phosphor green `#A7F432` accent locked** — went through 5 color picks (current orange, phosphor green, hot pink, violet, cyber yellow). User picked phosphor — terminal CRT heritage, terminal-first product fit, differentiates from every other dev-kit landing. Swapped site-wide: cursor + sparkle gradient + terminal shadow + Bento card glow + Pricing card glow + FinalCta twin glows + ::selection pseudo-element. Emerald-400 status dots stayed (they signal "live", not brand). **MetallicMark logo iterated 8+ times.** Final form: types `/shipkit▌` char-by-char on first load (140ms/char, 220ms paint delay), `/` slash in near-white `#E5E5E5`, `shipkit` wordmark in chrome white→gray gradient (white → `#6E6E6E`) via `bg-clip-text`, blinking phosphor cursor `<span>` (0.14em × 0.95em rect, `align: baseline`, `anim-cursor-blink` 1.05s `steps(1, end)`). Tried SVG-first w/ embedded `<text>` elements — bailed in favour of HTML+CSS because inline flow positions the cursor naturally as letters appear (no manual x-position math, no font-width measuring). User feedback drove every revision: nano-banana rendered icon attempts failed (chevron + slash floating disconnected, fat block cursor, color mismatches), text was too thin then too fat, slash blended w/ wordmark, cursor extended below baseline. Final spec landed via direct CSS dimension iteration. **Stripped all GitHub OAuth references** — kit is paid not open-source. "Star on GitHub" hero CTA → "See what's inside ↓" (#features anchor). "Sign in with GitHub" pricing CTA → "Get instant access — $29" (lock icon, LemonSqueezy footer). Folder/code-repo icon replaced the GitHub octocat on "You own the code" side card. "Private GitHub repo invite" body → "Private repo invite". **Hero size fix** — user reported headlines wrapping to 6 lines on desktop. Reduced scale across all h2/h3: hero `lg:text-[104px] → lg:text-[76px]`, feature/section h2s `lg:text-[72px] → lg:text-[60px]`, ShowDontTell h3 `lg:text-[60px] → lg:text-[52px]`, FinalCta `lg:text-[96px] → lg:text-[76px]`. Added `text-balance` to all headlines for nicer wrapping. Container widened `max-w-4xl → max-w-5xl` so 23-char phrases fit one line. **Animations layer added** — site-wide micro-motion: (a) `anim-grid-drift` translates the bg grid pattern diagonally over 120s (one cell per 2min — imperceptible per-frame, gives the page a "breathing" feel), (b) `anim-ambient` drifts a large phosphor-green radial blob (60vh × 70vw, blurred 60px, max 10% alpha) over 28s — feels like ambient CRT light bleeding, (c) `anim-card-lift` adds `translateY(-2px)` on hover with `cubic-bezier(0.22, 1, 0.36, 1)` easing — applied to FeatureCards bento, Pricing side cards, FAQ items, (d) Pricing side-card icon containers `scale(1.10) rotate(-3deg)` + bg/text flip to phosphor on group-hover, (e) Early-bird sparkle SVG spins 360deg over 14s, (f) `anim-cursor-blink` hard-blinks at 1.05s `steps(1, end)`. **Card hover glow** — all cards get phosphor border (`border-[#A7F432]/30`) + soft outer drop-shadow (`shadow-[0_0_50px_-12px_rgba(167,244,50,0.30)]`) on hover. FAQ open state amplifies the glow. **Sticky header fix** — user reported nav disappearing on scroll. Root cause: `overflow-x-hidden` on the outermost wrapper `<div>` was killing `position: sticky` on descendants (CSS limitation — sticky descendants don't work inside overflow:hidden ancestors). Removed `overflow-x-hidden`; ambient blob already had its own `overflow-hidden` clip wrapper that's `fixed`-positioned (outside flow), so the page still doesn't horizontally scroll. **Custom rAF smooth-scroll** — native CSS `scroll-behavior: smooth` has fixed timing (~300ms snap-fast). User wanted softer/longer easing. Built `SmoothScrollScript.tsx` — single document-level click listener intercepts `a[href^="#"]` clicks, runs a `requestAnimationFrame` loop over **900ms** with `easeInOutCubic`, offsets target by 72px for the sticky header, updates `history.pushState`, cancels mid-animation if user wheels/touches (natural override), restores native `scroll-behavior` after. Honours `prefers-reduced-motion`. **FAQ accordion rewrite** — was native `<details>` with multi-open. Converted to controlled React `useState<number | null>` — single-open accordion (clicking one auto-collapses the other). Smooth expand uses the **CSS grid-template-rows `0fr ↔ 1fr` trick** — animates true content height, no `max-height` guessing, transitions at 320ms ease-out. Chevron rotates `0 → 180deg` over 300ms with bg/border/text flipping to phosphor on open. **TerminalTyper hero** — converted the static `<pre>` block in the hero to an animated terminal player. IntersectionObserver triggers on 25% viewport entry. `$` command lines type char-by-char (60ms/char), prompt rows pop in instantly (380ms gap), mixed-color lines preserved (`✔` checks + dim `·` separator + answer, plus `✓ Done.` and `▸ Installed.` in phosphor). Initial version used `?` Inquirer prompt prefix — user pointed out that's only shown WHILE asking; real terminals show `✔` post-answer. Switched to authentic Inquirer-finished state: `✔ Rename the app · my-cool-app`. `min-h-[380px]` on terminal wrapper locks the size so it doesn't grow as lines appear. Phosphor-green CSS-drawn `<span>` cursor (2.5px × 1.25em, `align-middle + translate-y[0.15em]`) blinks throughout and parks at end. **Misc:** grid pattern softened (opacity `0.07 → 0.035`, size `60px → 72px`, mask tightened from `80% × 60% at 30%` to `60% × 40% at 20%`); Pricing side cards stretch via `flex flex-col gap-4` + `flex-1` so the column fills the main-card height; root layout metadata updated to new title "ShipKit — Ship your Android app. Without Android Studio." + matching OG/Twitter cards. **Status:** site visually + copy-locked. Routes: `/` (OSForms-style — new default), `/sleek` (archived Navy + Gold), `/forest`, `/neon`, `/orchid` (palette experiments). Build green throughout. Bundle: `/` 4.06 kB First Load JS (typing terminal + smooth-scroll script + accordion state). **Next: pending items unchanged from Day 1** — hero video (user records `/kit-start-setup` screencast), real screenshots, Nextra docs at /docs, GitHub OAuth via Auth.js (Day 3), LemonSqueezy webhook (Day 4), OG image + sitemap + Vercel deploy + DNS (Day 5).
- **2026-06-04 (cont. — Phase 7 site full design + copy pass, same day)**: After the morning scaffold landed (commit `2a6006e`) the user pushed the site through a full design + copy iteration arc — 12 follow-up commits in `../shipkit-site`. **Brand repaint to Navy + Gold (Midnight Sky)** — locked palette is `#00296B` navy chrome + `#FDC500` gold for CTAs/highlights only. Mid-blues + bright-yellow secondary shades stayed as Tailwind tokens but unused; the strict 2-color discipline reads "bold" instead of "rainbow SaaS". Typography swapped to **Geist Sans + Geist Mono** (Vercel, free) via `next/font`, exposed as `--font-geist-sans` / `--font-geist-mono` CSS vars; Tailwind `sans/display/mono` all resolve to Geist; `tracking-tightest -0.04em` preset added. Display scale: hero h1 `text-6xl → text-9xl` @ 800 with `leading-[0.95]`, section h2 `text-5xl → text-7xl`. Sleek-style multi-line headings with one accent word in gold. Hero gained an ambient gold radial glow blob. **UI UX Pro Max v2 (deep-ink + Bento)** — user feedback "too much blue" on navy v1 → repivoted to `ink-950 #020617` page bg (near-black w/ blue undertone, slate-950 family) + `ink-900 #0A0F1F` surface bands + `ink-800 #111827` cards + `ink-700 #1F2937` hover. Surface rhythm reads as bands instead of a continuous wall: Hero ink-950, PlatformStrip ink-900 (first break), TrustStrip ink-950, Features ink-950 page + ink-800 Bento cards, ShowDontTell ink-900 (second break), TechStack ink-950, Comparison ink-950 + ShipKit-column `bg-gold/[0.04]` tint, Pricing ink-900 (third break — pricing must feel different), FAQ ink-950, FinalCta ink-900 panel w/ twin gold corner glows, Footer ink-950. **Bento Features grid** — "Setup commands" gets `md:col-span-2` + "The Moat" badge (later renamed "The Magic") + larger type (`text-3xl md:text-4xl` heading, `text-lg md:text-xl` body, `p-12`) because it IS the differentiator vs other kits; AI ready + Permissions also span 2 cols; Auth / Paywall / Analytics in single-col tiles. **Gold-scarcity rule** — gold appears ONLY at CTAs (always), eyebrows, Comparison ShipKit ✓ marks + column tint, Hero glow + accent word, Pricing main-card border + `$29` price + corner glow, FinalCta accent word + corner glows, logo squares. v2 first stripped gold too aggressively (full-white headings felt overwhelming per user) → **one gold accent word per section h2** restored: FeatureCards "Skip the plumbing.", ShowDontTell per-section highlight via new `highlight` field, TechStack "the big apps use.", Comparison "Here's why.", Pricing "Lifetime updates.", FAQ "answered." (8 of 8 major sections). **Pricing balance pass** — sidebar had only 3 side cards (Cohort / 14-day refund / Unlimited apps), shorter than the main card. Added 2 more: "You own the code" (private GitHub repo, clone+fork+back-up forever — echoes the FAQ "what if ShipKaro disappears" answer) and "AI-coding ready" (Claude Code / Cursor / Windsurf compatible — differentiator most other kits don't surface). All 5 side cards use neutral white icon treatment per gold-scarcity rule. **PlatformStrip Coming Soon tooltips** — each non-available pill (iOS Native / Flutter / React Native / Web) wrapped in a `group` container; tooltip uses `group-hover:opacity-100` (pure Tailwind, no JS state). `cursor-help` on coming-soon pills telegraphs the interactive state; pill-wide hover affordance + `role="tooltip"` + `pointer-events-none`. First version showed quarterly dates (iOS Q3 2026 / Flutter Q4 2026 / RN Q4 2026 / Web 2027) — later dropped per user request (dates create unkeepable promises). **Tooltip visibility fix** — first cut was `bg-ink-950` (dark on dark inside surface-band ink-900, almost invisible). Pivoted to white pill: `bg-white text-ink-950 font-bold uppercase tracking-wider shadow-2xl shadow-black/70 ring-1 ring-black/20`, tail = solid white SVG triangle (not the half-border rotated-square trick), tiny `translate-y-1 → 0` lift animation. **FAQ → card style** — earlier divide-y white/8 rows felt "too white" per user. Replaced with separate `rounded-2xl border-white/10 bg-ink-800` cards (matches sitewide card system); open card lifts to `border-gold/20 + bg-ink-800/80`; chevron lives in its own 8x8 pill (clear hit-target + open/closed affordance); chevron open rotates + `border-gold/30 + bg-gold/10 + text-gold`; cards stack with `space-y-3`, no divider lines anywhere. **GitHub OAuth button on pricing** — replaced the gold "Sign in with GitHub to buy" pill with the **standard GitHub-style black button** (inline GitHub octocat SVG + white text + black bg + soft shadow). Recognition > novelty; reduces friction (matches every OAuth button users have seen). **Copy pass #1 (conversion-focused tightening)** — hero sub "Stop wiring auth, paywalls, and analytics… ship the app you've been putting off — this weekend"; TrustStrip footer replaced generic community/cohort line with **author credentials "indie Android dev since 2014, 12+ published apps, 300+ devs in the community"** (trust comes from track record, not adjectives); ShowDontTell #1 ends "no design system to maintain on top of the one you bought"; ShowDontTell #2 "This is the part other kits don't have"; ShowDontTell #3 "Real Google sign-in in 10 minutes." / "the native sheet, not a browser tab"; ShowDontTell #4 "Your privacy policy. Written by your code."; TechStack "Standard stack. No magic." + "If you've shipped one Android app, you already know everything here. We just saved you 80 hours of typing it."; Comparison "Build from scratch. Or skip the wiring."; Pricing "Pay once. Ship forever." + "$29 today. $0 every year after." + "yours to keep, even if we vanish"; FinalCta "30 spots at $29. Then $49 forever." ShowDontTell #2 gold accent moved from "Ship." to `/kit-start-setup` (mono, gold — the slash-command IS the differentiator); "40+ Compose components" → "40+ UI components"; FeatureCards h2 → "Build the features. Skip the plumbing." **Copy pass #2 — full vibe-coder rewrite** — user feedback: "in many places I'm seeing nerdy tech terms (SHA-1 etc). Remember this kit is for vibe coders. Focus on 3 things: AI agents ready (Claude/Cursor), end-to-end (design / Play Store / onboarding / paywall / auth / privacy), no Android Studio needed (all from terminal)." Heaviest rewrite of the day. **New hero headline: "Ship your Android app. Without Android Studio."** (was "Ship your Android app in a weekend"). Hero sub: "Auth, payments, analytics, Play Store upload — all done by your AI agent from the terminal. Type /kit-start-setup, answer a few questions, ship by lunch." **FeatureCards h2 → "Your AI handles everything. You ship."** 6 cards retitled: Setup commands → "AI-agent commands" (showcase tile, badge renamed to "The Magic"); Auth → "Login" ("5 minutes, no keys to copy-paste"); Paywall → "Payments"; Analytics → "Usage + crashes"; AI → "AI features built-in" ("ChatGPT, Claude, Gemini — 100+ models in one place"); Permissions → "Permission screens" ("Pretty 'allow camera/photos/etc' screens, already built and styled"). **ShowDontTell SHA-1 strip** — user screenshot showed SHA-1 / Credential Manager / OAuth / Play App Signing jargon. Rewrote #3 Auth: "Google sign-in. **Without the headache.**" body "Type /kit-setup-auth. Your AI walks you through it like a wizard — paste two keys, done. Your users tap 'Continue with Google', they're in." All four ShowDontTell sections rewritten to drop "shimmer, skeletons, Material 3 native" / "Gradle errors at midnight" / "lawyer copy-paste" jargon. **TechStack** h2 → "Built on what **the big apps use.**" Sub: "Same tools the apps in your phone use. So when you Google a problem at 2am, there's an answer." Chip list reordered to lead with logos-they-know (Kotlin / Compose / Material 3 → Supabase / Firebase / RevenueCat / PostHog / Sentry / OpenRouter / Crashlytics → utility libs with hint parens "Room (database)", "DataStore (settings)", "Koin (wiring)", "Timber (logs)"). Dropped "AboutLibraries". **Comparison** h2 → "Why ShipKit? **Here's why.**" Row labels rewritten: "Pure Android Native (no KMP overhead)" → "Real Android (not a web wrapper)"; "Setup via terminal commands" → "Set up from your terminal — no Android Studio"; "Codebase-driven privacy policy generator" → "Generates your privacy policy from your code"; "8 full-screen permission flows" → "Walks you through Play Store upload"; **NEW row** "Works with Claude Code / Cursor / AI agents" (true: ShipKit, false: scratch + other kits); "Time to first build" → "Time to first published app" (values "1–2 months / 1–2 weeks / A weekend"). **Pricing What's-included** — 13 lines from tech jargon to plain English ("Auth (Supabase + Firebase + Google sign-in)" → "Google + email login"; "Paywall (RevenueCat) + entitlements" → "In-app payments + subscriptions"; "Analytics (PostHog + Firebase + Crashlytics + Sentry)" → "Usage tracking + crash reports"; "AI integration (OpenRouter, 100+ models)" → "AI features built-in (100+ models, one key)"; "Codebase-driven privacy policy generator" → "Auto-generated privacy policy"). **FAQ rewrite** — 11 answers from tech-dev tone to vibe-coder tone. New opener Q "I don't really know how to code. Can I still use ShipKit?" (answers the unspoken first concern). New "Do I need to open Android Studio?" (answers the #2 fear). "Do I get support?" → "What if I get stuck?" (humanises). Dropped Compose / OAuth / Credential Manager / Play App Signing / KitConfig / Retrofit / Supabase Storage references. "Why OpenRouter and not OpenAI directly?" rewritten without "wire-compatible" / "API base URL" jargon. **Hero one-viewport pass + regression + final shape** — first attempt at "fit hero in one viewport on desktop" used `flex min-h-screen flex-col` + h1 `text-5xl → text-[6.5rem]` + 2-line headline + `flex-1 items-end` video + `max-w-3xl lg:max-w-4xl`. Then user reported h1 at 104px was overflowing the container, wrapping across multiple lines, video squeezed below the fold. Fixed by dropping h1 one notch at every breakpoint (`text-4xl/5xl/6xl/[5rem]/[5.5rem]`), tightening sub, centering video vertically (`flex-1 items-center justify-center`), clamping video to a single `max-w-4xl`. **THEN** user said "It used to be big card." — reverted to natural-flow layout that lets the video be the prominent element: dropped `min-h-screen + flex-1`; hero now `pt-24 lg:pt-28 + pb-16 lg:pb-24` normal flow; video back to `max-w-6xl` (original big card); video icon/copy bumped back to `p-8 + 48px play icon`; text block keeps the tighter sizing from the fit-in-viewport pass so headline still fits two lines. Trade-off accepted: on 1080p the video may extend slightly past the first viewport — big video is the point. **Header — real shipkaro mark + "by shipkaro" badge** — initial header had purple-gradient placeholder + 🚢 emoji. Downloaded real shipkaro mark from substack CDN → `public/shipkaro-mark.png` (500x500 RGBA, 22KB). Replaced placeholder with `<img src="/shipkaro-mark.png" />` at 16x16. Attribution chip now its own `<a target="_blank">` to `https://shipkaro.dev` (was nested inside ShipKit home link → would have leaked clicks to "#"). Restructured header: left = ShipKit wordmark + attribution chip in a flex group; middle = nav; right = CTA. Outer container `justify-between + gap-6`. Hover state on chip: `border-white/20 + bg-white/[0.06] + text-white/80`. **Build green throughout all 12 commits.** Bundle ~87.1 kB First Load JS unchanged. **Status:** site visually + copy-locked for Day 1. Pending: hero video user-records, real screenshots (Components catalog light+dark, /kit-start-setup recording, Auth flow, generated privacy policy), Nextra docs at /docs (Day 2), GitHub OAuth via Auth.js (Day 3), LemonSqueezy webhook (Day 4), OG image + sitemap + Vercel deploy + DNS (Day 5). **Next: user device-tests the Android kit tomorrow + Phase 7 Day 2 work.**
- **2026-06-04 (Phase 7 sales+docs site bootstrap + /kit-generate-screenshots)**: Brainstormed Phase 7 with the user — landing page shape, positioning, pricing, buyer journey, infra decisions. **Locked decisions:** brand name = **ShipKit**; positioning = pure-Android-native + cohort/community (lean into what kappmaker/SwiftStarterKits don't have); hero = autoplay video of `/kit-start-setup` running (user records the speed-up); features section = hybrid (6-card grid on top + 4 show-don't-tell screenshot sections below); pricing = **$29 early-bird first 30 → $49 regular** (one-time, lifetime updates); cohort path = **LemonSqueezy 100%-off coupon code per cohort** (drop in WhatsApp at kickoff, same checkout flow as paid buyers — auto repo-add webhook, no manual work); 14-day money-back guarantee; "other kits" generic comparison (no named competitors); demo/proof = public docs + screenshot gallery + 90s screen recording in hero; community = **WhatsApp only** for now (Discord deferred until standalone buyers ask); domain = `kit.shipkaro.dev` subdomain (cleaner separation than `shipkaro.dev/kit`); payment provider = **LemonSqueezy** (MoR, easy API, devs love it; user will set up LemonSqueezy product + key later); repo location = **new dedicated private repo `shipkit-site`** (not inside the android-kit repo). **Multi-platform teaser:** user requested "Coming Soon" pills for iOS Native / Flutter / React Native / Web so buyers see the bigger picture (one kit per platform, future purchases). **Bootstrapped Phase 7 Day 1 work:** new directory at `../shipkit-site/`, Next.js 14 app-router + Tailwind 3 + TypeScript scaffold (hand-rolled, no shadcn CLI dep, no `create-next-app` interactive). Brand purple `#7C3AED` matches the Android kit's `BrandPrimary`. Landing page composed of 12 components in `components/`: Header (sticky nav + Get-the-kit CTA), Hero (h1 + sub + 2 CTAs + autoplay video placeholder pinned for user to drop `public/hero.mp4`), PlatformStrip (Android Available + 4 Coming Soon pills with status dots), TrustStrip (ShipKaro 300+ + cohort signal), FeatureCards 6-grid (Auth / Paywall / Analytics / AI / Permissions / Setup commands), ShowDontTell 4 alternating screenshot sections (placeholders for Components catalog, /kit-start-setup recording, Auth flow, generated privacy policy), TechStack pill row (17 libs from Kotlin to AboutLibraries), Comparison table (vs Build-from-scratch / Other kits / ShipKit — 9 rows, no named competitors), Pricing card ($29 early-bird first 30 + cohort + 14-day refund + multi-app license — three side-cards), FAQ accordion (10 Qs covering refund, multi-app, updates, other-platforms, what's-inside, OpenRouter, support, Windows-Linux, maintainer-disappearance), FinalCta ("Stop perfecting. Start shipping." — ShipKaro motto), Footer (4 columns: Product / Community / Legal + Pakistan-builder line). Configs: `next.config.mjs` (strict mode), `tailwind.config.ts` (brand-{50..950} purple scale + scroll-smooth), `tsconfig.json` (strict + `@/*` path alias), `postcss.config.mjs`, `.eslintrc.json` (next/core-web-vitals), `.gitignore` (Next.js + node_modules + .vercel + .env.local). README documents day-by-day phase plan, TODO list (hero video, real screenshots, Nextra docs at /docs, Auth.js GitHub OAuth, LemonSqueezy webhook, OG image + sitemap, Vercel deploy + DNS), and `npm install / npm run dev` instructions. Initial commit `2a6006e` (1036 insertions, 23 files). Pushed to **https://github.com/wajahatkarim3/shipkit-site** as a private repo via `gh repo create shipkit-site --private --source=. --push`. **Also earlier 2026-06-04: new standalone `/kit-generate-screenshots` command** — promoted from inline-in-/kit-upload-on-google-play to first-class so devs can refresh shots without re-walking the release flow. 3 steps: Step 0 detect existing PNGs (count + keep/regenerate/add-more branch); Step 1 generate-via-skill (invokes `aso-appstore-screenshots` w/ Play Store dimensions 1080×1920 portrait, 4–6 default count) OR provide-own (drop PNGs at `playstore/screenshots/<n>.png`). Mentions tablet + Wear shots only if dev asks. `/kit-upload-on-google-play` Step E (first-version + update paths) now invokes `/kit-generate-screenshots` inline instead of inlining the generate/provide branch. `/kit-start-setup` outro optional-commands list gains the new command. **Next: Phase 7 Day 2 (Nextra docs scaffold + content migration from shipkaro-1dayapp-docs); user will device-test the Android kit tomorrow.**
- **2026-06-02 (cont. — 8 full-screen permission flows)**: Spotted in SwiftStarterKits' docs that they ship dedicated per-permission "view" screens (Notifications / Contacts / Calendar / Location / Camera / Microphone / Photo / Tracking / MusicLibrary / Motion). Our kit only had primer components (bottom-sheet rationale), not full screens. Built the 8 Android-relevant variants; iOS-only Tracking ATT + Music Library skipped (no Android equivalent). **New parameterised `KitPermissionScreen` composable** (`core/designsystem/permission/`) — full-screen flow: icon chip → title → rationale → Allow + Skip. Auto-swaps "Allow" → "Open Settings" + body → denied-state rationale when the OS permission is permanently denied, so the user has a forward path after the OS suppresses the second dialog. Calls back via `onGranted` / `onSkip`. **Eight pre-instantiated screens** at `feature/permissions/PermissionScreens.kt`: `NotificationsPermissionScreen` (POST_NOTIFICATIONS, 33+), `CameraPermissionScreen` (CAMERA), `MicrophonePermissionScreen` (RECORD_AUDIO), `PhotoPermissionScreen` (READ_MEDIA_IMAGES 33+ / READ_EXTERNAL_STORAGE legacy), `ContactsPermissionScreen` (READ_CONTACTS), `CalendarPermissionScreen` (READ_CALENDAR), `LocationPermissionScreen` (ACCESS_FINE_LOCATION), `MotionPermissionScreen` (ACTIVITY_RECOGNITION, 29+). **`KitPermission` enum extended** with MICROPHONE, PHOTO, CONTACTS, CALENDAR, MOTION. **`KitPermissionState` SDK branching:** PHOTO swaps manifest string to `READ_EXTERNAL_STORAGE` on API < 33; MOTION auto-grants on API < 29 (ACTIVITY_RECOGNITION didn't exist). Notifications-implicit-grant path generalised into a single `implicitlyGranted` flag covering both POST_NOTIFICATIONS + ACTIVITY_RECOGNITION SDK gates. **`KitIcons` interface gains 5 new slots** (`microphone`, `photo`, `contacts`, `calendar`, `motion`) wired across all 3 packs — Material (Mic / PhotoLibrary / Contacts / CalendarMonth / DirectionsRun), Feather (Mic / Image / Users / Calendar / Activity), Tabler (Microphone / Photo / Users / Calendar / Activity). 32 new permission strings (8 × {title, rationale, denied}) + 4 shared button labels (allow / enable / skip / open-settings); removed earlier legacy `permission_allow` / `permission_not_now` / `permission_open_settings` strings plus the duplicate `permission_*_title` / `_rationale` entries from the old primer section. `KitPermissionState` KDoc updated to point at the new keys. **AndroidManifest opt-in declarations:** 8 `<uses-permission>` lines added, all commented out by default — uncomment per feature your app actually uses (leaving unused perms on a release bloats Play Console Data Safety). **Skipped per scope:** Account-management screens (ChangePassword / ChangeEmail / VerifyPassword / VerifyEmail / ResetPassword — Bucket B, deferred until a real app needs them); iOS-only ATT + Music Library; catalog inline preview (full-screen flows don't compose in a scrolling column without Dialog wrappers — devs reference the screen files directly). `:app:assembleDebug` green. **Next: device-test permission flows + everything else from today.**
- **2026-06-02 (cont. — runtime testing pass, pacing rewrites, OSS licenses swap, env check)**: Heavy device-test feedback day from the user (still on the post-/kit-start-setup flow). Fixed two correctness bugs, two crashes, four UX/pacing issues, and added two new commands. **TaskList "&amp;" rendering bug:** Step titles `Brand & theme` + `Build & run` in `/kit-start-setup` showed as `Brand &amp; theme` / `Build &amp; run` because TaskList HTML-encodes `&` in task subjects. Swapped to `Brand and theme` / `Build and run`. **/kit-setup-auth Sub-step 4a pacing:** the old block dumped Supabase keys + Google provider toggle + redirect URL + SHA-1 + Google Cloud OAuth client in one message, then ran `./gradlew signingReport` immediately. Non-coder lost place. Restructured into 7 paced sub-steps with explicit "STOP and wait" gates and no shell commands run before explicit "yes". Same shape applied to Step 4b (Firebase, 5 sub-steps). **/kit-setup-auth Supabase + Google flow correctness:** earlier copy said "Copy the Authorized Client ID of type Web application" from Supabase — Supabase doesn't generate it; Google Cloud Console does, then you paste it INTO Supabase. Real flow needs two Google Cloud OAuth clients (Web for Supabase to talk to Google + Android for the on-device Credential Manager sheet w/ SHA-1). Rewrote Step 4a Google branch into 6 sub-steps: OAuth consent screen → Create Web OAuth client (Supabase callback as Authorized redirect URI) → Connect Supabase to that Web client (paste Client ID + Secret) → Add in-app redirect URL `shipkaro://auth-callback` → Grab debug SHA-1 (wait for "yes" before `signingReport`) → Create Android OAuth client w/ package name + SHA-1. **/kit-upload-on-google-play Step D (release SHA-1 register):** added missing release-time Google sign-in step. Detects `KitConfig.GOOGLE_SIGN_IN_ENABLED` + `AUTH_PROVIDER`; skips entirely if Google sign-in is off. 3 sub-steps: D.1 pick Play App Signing vs self-managed (Play App Signing on first upload = skip + return after K); D.2A Play App Signing path (SHA-1 from Play Console → App integrity); D.2B self-managed path (Claude runs `keytool` after explicit "yes"); D.3 register in Google Cloud Console (Supabase) or Firebase (FIREBASE, includes re-download of `google-services.json`). Task list renumbered D–K; body cross-refs updated. **/kit-setup-paywall RC dashboard correctness:** old text said "Project Settings → Apps → New app" — Apps + API keys are NOT under Project Settings anymore. They're top-level left-sidebar items. Rewrote Step 2 into 6 paced sub-steps including: sidebar Apps → New app configuration → Google Play Store form (App name + package name + Service Account Credentials JSON empty for now); Product catalog → Entitlements → premium; sidebar API keys → copy `goog_...` → write `revenuecat.android.api.key` to `local.properties`; pick SOFT/HARD; **release-time info block** (read now, do later) covering Play Console products, service account JSON via https://www.revenuecat.com/docs/platform-resources/google-platform-resources (IAM service account + Play permissions + JSON upload + 36h propagation), and offerings + paywall design in RC's visual editor. References https://www.shipkaro.dev/mobile-docs/features/in-app-purchases for the older kit's step-by-step. **/kit-setup-analytics pacing:** same dump-everything bug — PostHog + Firebase Crashlytics in one message. Split Step 2 (PostHog) into 2.1 create / 2.2 paste-token, Step 3 (Firebase delegated to /kit-setup-firebase), Step 4 (Sentry) into 4.1 create / 4.2 DSN-paste. Added pacing-rule paragraph at top. **/kit-start-setup Step 7 Ops nuked:** for non-coders shipping a v1, the remote-config / update-gate / push decisions are theoretical (no users yet). Kit ships `RemoteAppConfig.LOCAL` by default so the gate compiles + runs offline. Dropped Step 7. Step 8 (Build and run) promoted to Step 7. Step 0 feature-list bullet for Ops removed. Wrap-up gained a verbatim outro listing the 7 optional commands devs can run later (`/kit-setup-updates`, `/kit-setup-review-dialog`, `/kit-setup-ai`, `/kit-translate`, `/kit-design-onboarding`, `/kit-design-app`, `/kit-upload-on-google-play`). `/kit-setup-updates` stays standalone, unchanged. **Sentry auto-init crash fix:** Sentry's `SentryInitProvider` ContentProvider runs at process start (before `KitApplication.onCreate`), reads AndroidManifest meta-data for a DSN, finds none, throws `IllegalArgumentException("DSN is required.")` — crashing the app for any clone where `sentry.dsn` is blank. Added `<meta-data android:name="io.sentry.auto-init" android:value="false" />` to `<application>` so the SDK only inits when `KitApplication` calls `SentryAndroid.init` manually behind both gates. **OSS Licenses screen blank — swapped to mikepenz/AboutLibraries:** Google's `oss-licenses-plugin` only scans Google Play Services / Firebase artifacts; the kit's deps (Compose, Retrofit, Koin, Coil, Timber, RevenueCat, Supabase, etc.) don't publish the `third_party_licenses` resource it relies on, so the screen rendered an empty list. Replaced with `com.mikepenz.aboutlibraries.plugin:11.6.2` + `aboutlibraries-compose-m3:11.6.2`. Plugin scans the full Gradle dep tree at build time → emits `aboutlibraries.json` asset. New `feature/licenses/OssLicensesScreen.kt` drops `LibrariesContainer` into a Scaffold + TopAppBar. `Route.OssLicenses` added; `KitNavHost` wires it as a post-auth route. `SettingsScreen` takes a new `onOssLicenses` callback instead of launching `OssLicensesMenuActivity`. Removed: `play-services-oss-licenses` lib, `oss-licenses` plugin, `settings.gradle.kts` plugin-resolution mapping (AboutLibraries publishes a plugin marker). **Step 0 — Detect existing state across all 8 /kit-setup-* commands:** previous behaviour was "restart from scratch" — re-running a setup command re-asked every question even if the work was done (PostHog key already in `local.properties` got re-pasted). Each `/kit-setup-*` command now opens with a `Step 0` that reads `KitConfig.kt` + `local.properties` + relevant files, then branches: fully configured → AskUserQuestion (keep-as-is / reconfigure / change one thing — with one-thing options that jump straight to the matching sub-step); partially configured → skips already-done sub-steps and walks only the missing ones; nothing configured → walks the full flow. Per-command checks documented in CLAUDE.md decisions below. Interrupted mid-flow runs now resume from where they left off. **/kit-env-check (new, cross-platform):** built per-machine env probe. Checks JDK 17, `ANDROID_HOME`, `adb`, android CLI, Android Skills (5 required) + scrcpy + Holo (2 optional). `uname` → branches install commands per OS: macOS (brew + curl|sh), Windows (Scoop + PowerShell SetEnvironmentVariable + `cargo install holo` since the holo install.sh is bash-only), Linux (apt + curl|sh). Doesn't duplicate `/kit-start-setup` Step 0 — that stays per-project (`local.properties` + SDK path + Gradle wrapper); env-check is per-machine. References both `shipkaro.dev/mobile-docs/getting-started/setup-macos` and `.../setup-windows` pages. Wire-ins: `/kit-start-setup` Step 0 prereq block top ("if anything below fails, run /kit-env-check first") + outro optional-commands list. Holo (measure-sh/holo) added as recommended terminal-first Android profiler for the cohort audience that skips Android Studio. **Components catalog screen + 20 new components**: see earlier 2026-06-02 entry below for the SSK-parity pass details. `:app:assembleDebug` green throughout. **Next: user device-tests tomorrow (their words).**
- **2026-06-02 (SwiftStarterKits-parity sweep — 20 new components)**: Compared kit's design system against SwiftStarterKits (swiftstarterkits.com claims "100+ SwiftUI components") + the common SwiftUI starter-kit component universe. The site's marketing didn't enumerate the 100 by name, so worked from the SwiftUI-standard component set + SSK's listed categories. Mapped each iOS component to its Android equivalent (skipped iOS-only items like dynamic app icons, Apple sign-in, Liquid Glass) and shipped the 20 missing ones. **Snackbar w/ severity styles (inspired by TonnyL/Light)** — `KitSnackbarStyle` enum (INFO/SUCCESS/WARNING/ERROR), `KitSnackbarVisuals` SnackbarVisuals subclass, `KitSnackbarController` w/ `.info()/.success()/.warning()/.error()` helpers, `rememberKitSnackbarController()` factory, `KitSnackbarHost` host that picks the kit-styled rendering when visuals are `KitSnackbarVisuals` and falls back to Material default for plain strings. Each style maps to the matching `KitTheme.colors` semantic color + `KitIcons` icon. Error duration defaults to LONG; others SHORT. Action callback only fires on `ActionPerformed`. Previous snackbar auto-dismissed when new one shows. **Pickers + advanced inputs (7):** `KitSlider`, `KitStepper`, `KitSegmentedButton` (single-choice segmented row), `KitDatePicker` (modal) + `KitInlineDatePicker`, `KitDropdownMenu` (exposed dropdown — generic `<T>`), `KitSearchField` (magnifier + clear button), `KitOtpField` (N-cell code entry, transparent BasicTextField over rendered cells). **List patterns (3):** `KitSwipeableRow` (SwipeToDismissBox wrapper, both directions, state auto-resets), `KitDisclosureGroup` (expandable group, stateless + stateful overloads), `KitLazyGrid` + `KitAdaptiveLazyGrid` (fixed + adaptive columns). **Feedback (4):** `KitPullToRefresh` (Material 3 wrapper), `KitLinearProgress` + `KitIndeterminateLinearProgress`, `Modifier.kitShimmer()` (looping linear gradient sweep), `KitSkeleton` + `KitSkeletonListItem`. **Misc UI (3):** `KitBadge` (number / text / dot overlay via BadgedBox), `KitRatingBar` (5-star tappable, tap same star to clear), `KitFab` (single + extended). **Layout scaffolds (1):** `KitBottomNavScaffold` + `KitBottomNavBar` + `KitBottomNavItem` (badge support). **Webview (1):** `KitWebView` — embedded; JS off by default. **Utils (2):** `EmailLauncher.send()` (`mailto:` intent w/ chooser + subject/body), `KitHaptics` + `rememberKitHaptics()` (semantic helpers `lightTap`/`mediumTap`/`longPress`/`success`). **Catalog screen** got 7 new sections wiring every addition for live preview: Pickers, List patterns, Progress & shimmer, Badges & rating, Scaffolds, Utilities, Snackbars (toasts). 40+ new `catalog_*` strings + 2 generic `kit_datepicker_*` strings. **Skipped (iOS-specific or rejected):** dynamic app icons (already rejected), Apple sign-in, range slider, ColorPicker, calendar (Material DatePicker covers), confetti/animation effects (too app-specific), sound effects toggle. `:app:compileDebugKotlin` + `:app:assembleDebug` green. Initial slider file had cruft (leftover `dp` helpers) — rewrote cleanly. Initial `KitDropdownMenu` called `ExposedDropdownMenu` from the wrong scope — fix was to call it inside the `ExposedDropdownMenuBox` content lambda where it's an extension on `ExposedDropdownMenuBoxScope`. `KitWebView` JS opt-in safety note added to KDoc — Custom Tabs preferred for external links. **All 20 components shipped without device test — runtime verification pending.** **Next: device-test the new catalog sections + Snackbar API ergonomics.**
- **2026-06-01 (Components catalog screen, same day)**: New `feature/catalog/ComponentsCatalogScreen.kt` — live preview of every kit component (KitButton 6 variants, KitTextField + KitPasswordField + error state, KitChip + KitFilterChip, KitCard plain + clickable, KitListItem, KitBanner 4 styles + dismissible, KitAvatar initials/url/fallback, KitPricingCard 3-tier row, KitFeatureRow + KitBulletRow, KitInlineLoading / KitEmptyState / KitErrorState / KitSuccessState, AccountRow / ToggleRow / NavRow / DangerRow / LegalLinks / SettingsDivider / SettingsSection, KitDialog plain + destructive, KitBottomSheet, KitUpdateSheet required + optional, KitWhatsNewSheet). Pure dummy data; nothing mutates app state. Reachable from Home → "Browse kit components" primary button (above the existing "Open settings"). `Route.ComponentsCatalog` added + wired in `KitNavHost` (treated as post-auth for sign-out redirect). 80+ new `catalog_*` strings. File scope-marked `@file:OptIn(ExperimentalMaterial3Api::class)` because `KitBottomSheet` exposes `SheetState` via default param. `:app:assembleDebug` green. Purpose: sanity-check screen for devs to confirm the brand color + icon pack picks land cleanly across the design system; can be deleted in three places (screen + route + Home button) when the dev no longer needs it.
- **2026-06-01 (Sentry pass, same day)**: Added Sentry as a parallel-tree alternative / addition to Firebase Crashlytics. `io.sentry:sentry-android:7.20.0` + new `core/log/SentryTree.kt` symmetric with `CrashlyticsTree`. Timber supports any number of trees so both run side-by-side when picked together — INFO/WARN become Sentry breadcrumbs, ERROR/ASSERT call `captureException` (with throwable) or `captureMessage(SentryLevel.ERROR)`. `KitConfig.SENTRY_ENABLED` (default false) + `BuildConfig.SENTRY_DSN` from `local.properties` (`sentry.dsn`). `KitApplication.onCreate` initialises `SentryAndroid` + plants `SentryTree` only when both gates pass — inert by default. `/kit-setup-analytics` Step 1 multi-select adds Sentry; new Step 4 walks the DSN paste flow + warns when both Crashlytics + Sentry get picked together (each crash lands in both dashboards, which is sometimes desired). `local.properties.template` updated. Picked Sentry because teams using it usually want richer breadcrumbs / release-health than Crashlytics offers, OR crash reporting without Firebase + `google-services.json`. `:app:compileDebugKotlin` green. Commits 91d59e0 + 98dc11a pushed.
- **2026-06-01 (feature parity sweep — Timber, OpenRouter, SecureDataStore, OSS licenses, 4 new setup commands)**: Reviewed feature matrix against kappmaker.com / swiftstarterkits.com / shipnative.app and closed seven gaps in one pass. **Logging:** added Timber (`com.jakewharton.timber:timber:5.0.1`) + `core/log/CrashlyticsTree.kt`. `KitApplication.onCreate` plants `DebugTree` in debug and `CrashlyticsTree` in release (gated on `KitConfig.ANALYTICS_ENABLED` to match `AnalyticsManager.logError`'s rule). `CrashlyticsTree` mirrors INFO+ to `Crashlytics.log()` and routes ERROR/ASSERT to `recordException()`. Replaced the only existing `android.util.Log.w` call (`CustomTabs.kt`). **Rate button:** Settings → Rate now opens the Play Store listing via new `core/util/PlayStoreLauncher.kt` (`market://details?id=<applicationId>` with `https://play.google.com/store/apps/details?id=…` fallback). The in-app review dialog was a Play-quota-throttled silent-no-op in that slot; `InAppReviewManager` + its Koin binding stay untouched for the new `/kit-setup-review-dialog` command, which wires `requestReview(activity)` at one of four trigger styles (Nth launch / key action / time delay / manual call-site) with a `reviewPrompted` DataStore flag so it fires once. **SecureDataStore:** new `core/security/SecureDataStore.kt` — AES-256/GCM with master key in `AndroidKeyStore` (`KeyGenParameterSpec` `BLOCK_MODE_GCM` + `ENCRYPTION_PADDING_NONE` + `setRandomizedEncryptionRequired(true)`), ciphertext + 12-byte IV packed + Base64'd into a dedicated DataStore Preferences file `kit_secure_prefs` (separate from `kit_settings`). Single Koin singleton in `coreModule`. Zero new deps — uses `javax.crypto` + AndroidX Keystore APIs only. Docs note that Supabase / Firebase / RevenueCat already encrypt their own tokens, so this is for dev-held secrets (user-entered API keys, custom OAuth refresh tokens from third-party backends). Skipped `EncryptedSharedPreferences` — AndroidX deprecated it Jul 2024. **OpenRouter AI:** new `core/ai/` package with `OpenRouterAiClient` (Retrofit interface), `OpenRouterAiRepository` (public surface), `OpenRouterAuthInterceptor` (bearer-token header + `X-Title`), `AiModels.kt` (`AiMessage` + wire DTOs). Methods are provider-generic and **model-first**: `generateText(model, prompt, systemPrompt?)`, `generateTextWithMessages(model, messages, temperature?, maxTokens?)`, `streamText(model, prompt, systemPrompt?)` (Flow<String> SSE chunks), `generateJson(model, prompt, jsonSchemaName?, schema?)`. **No "chat" naming** — these are AI-call methods for any purpose, not a chat-app. **No sample screen** — plumbing only. The OpenRouter Retrofit is a **separate named singleton** `retrofit_openrouter` so its base URL (`https://openrouter.ai/api/v1/`) never collides with the app's own `KitConfig.API_BASE_URL`. Future siblings (`TogetherAiRepository`, `GroqAiRepository`, `OpenAiRepository`) each get their own client class — no `AiClient` interface bottleneck. `KitConfig` adds `OPENROUTER_ENABLED` (default false) + `OPENROUTER_DEFAULT_MODEL` (default `meta-llama/llama-3.2-3b-instruct:free`). Key in `local.properties` → `BuildConfig.OPENROUTER_API_KEY`. `aiModule` registered only when `OPENROUTER_ENABLED` is true. New `/kit-setup-ai` command. **`/kit-plan-release-analytics`:** strategy-side analytics command, separate from `/kit-setup-analytics` (which stays SDK-plumbing only). Scans the unreleased `CHANGELOG.md` section + existing `AnalyticsEvents.kt` + `Route.kt`, derives release-specific funnel-goal options for `AskUserQuestion`, suggests 3–5 events with field types, then **auto-inserts** `analytics.logEvent(...)` at Grep'd call-sites (heuristic by event-name shape: `*_VIEWED` → screen `LaunchedEffect`, `*_STARTED` → button onClick, `*_COMPLETED` → VM success path, `*_ERROR` → catch path). Ambiguous matches (0 or 3+) ask the dev to confirm. Updates `AnalyticsEvents.kt` + `AnalyticsParams.kt`. Prints a verbatim PostHog funnel-build block. Wired into `/kit-upload-on-google-play` as Step G (first-version) + Step F (update). Hybrid auto-insert because the kit's audience is non-coder. **`/kit-generate-legal`:** scans `libs.versions.toml` + `KitConfig` + Retrofit `@GET/@POST/@PUT/@DELETE/@PATCH` annotations + Supabase `client.from()` table accesses, asks the dev 8 legal questions (company name, contact email, jurisdiction, EU/GDPR scope, California/CCPA, COPPA, retention window, server region), and writes three files into `playstore/`: `privacy_policy.md` (source of truth), `privacy_policy.html` (self-contained styled page for GitHub Pages / Vercel / Netlify hosting), and `play_data_safety.md` (filled form answers). The existing `legal/play-data-safety.md` stays as a template reference. ToS stays template-only — no codebase signal makes it dev-specific. Phase 7 landing page will render the same `.md` via MDX later. Wired into `/kit-upload-on-google-play` first-version Step F + update Step A (re-gen on SDK change). **OSS licenses screen:** added Google's `com.google.android.gms:oss-licenses-plugin:0.10.6` + `play-services-oss-licenses:17.1.0`. Plugin scans deps at build time → bakes licenses into raw resources → `OssLicensesMenuActivity` renders them. Settings → About → "Open source licenses" launches it with localized title. Plugin doesn't publish a plugin-marker artifact so `settings.gradle.kts` got a `resolutionStrategy.eachPlugin` block mapping the ID to `com.google.android.gms:oss-licenses-plugin:<version>` for catalog `alias()` syntax. `:app:assembleDebug` green. **All seven items shipped without device test — runtime verification pending.** **Next: user device-tests Timber + Crashlytics breadcrumbs + rate-button → Play listing + OSS licenses screen + the four new commands.**
- **2026-05-31 (paywall + auth + splash sweep; iterative bug fixes)**: Another runtime-driven pass after the user device-tested with a non-coder and ran setup himself. **Paywall: nuked the custom UI**; the kit now uses RevenueCat's prebuilt `Paywall` composable from `com.revenuecat.purchases:purchases-ui:9.23.1` (which IS standard Jetpack Compose for Android — the earlier CLAUDE.md "Compose Multiplatform conflict" note was wrong). `PaywallScreen.kt` is now ~50 lines wrapping `Paywall(options)` with a `PaywallListener`; design + copy live in the RC dashboard. Deleted `PurchaseViewModel.kt` (RC handles state), its Koin registration, and all `paywall_*` strings; `billing_error_*` strings kept (still used by `PurchaseManager` for programmatic errors). **Auth: dropped Stub from `/kit-setup-auth` question** — only Supabase (recommended) + Firebase shown; Stub stays as an internal fallback. Flipped `KitConfig.AUTH_ENABLED` default from true to false so a fresh clone runs without an auth screen; `/kit-setup-auth` Step 2 flips it back to true when the dev picks a real provider. **Splash: nuked the in-app `SplashScreen.kt`** — the Android OS cold-start splash already covers the gap. `KitNavHost` resolves the start route in a `LaunchedEffect(Unit)` during initial composition, renders a blank Box with the theme background while resolving (system splash still visible), then composes `NavHost(startDestination = resolved)`. Removed `Route.Splash` + the composable + the splash directory. **Delete-account blank screen** (race condition): removed the `LaunchedEffect(deleteStatus == Done) { onBack() }` in `SettingsScreen` — KitNavHost's sign-out redirect was already popping Home + Settings, and the redundant `popBackStack()` was popping the just-pushed Auth, leaving an empty stack and a blank screen. **Theme: dark-mode + container variants now derive from brand color** — `/kit-setup-theme` Step 1 computes `LightPrimaryContainer`, `LightOnPrimaryContainer`, and all four Dark variants from `BrandPrimary` via HSL targets (was hardcoded purple). **Icon pack picker** added to `/kit-setup-theme` Step 2: AskUserQuestion with Material (default) / Feather / Tabler / "Pick another" (lists Simple Icons, Font Awesome, Eva, Octicons, Line Awesome, Linea, Weather, CSS GG). Each option shows the pack's preview URL. "Apply" step disables unused packs reversibly by commenting catalog/build lines and wrapping the impl-file content in `/* ... */`. **Brand mark replaced**: downloaded the real ShipKit logo PNG from substack CDN (converted from WebP via `sips`, 300×300 RGBA), replaced the `ic_shipkaro_mark` vector. Adaptive-icon foreground now insets the same PNG by 19 % so it sits inside the safe zone. `ic_launcher_background` switched from #2962FF to `BrandPrimary #7C3AED`. **Free-app paywall leak**: `/kit-start-setup` Step 5 free-app branch now sets `PAYWALL_ENABLED = false` before skipping; previously the orchestrator only skipped the question, so the kit's nav still routed through Paywall after Auth on first launch. **`.debug` `applicationIdSuffix`** removed (already covered 2026-05-23, but resurfaced — verified gone). **Onboarding-questionnaire skill** bundled inline at `.claude/skills/onboarding-questionnaire/` (Calm/Headspace-style 10–14 screen flow generator, Compose native, scoped to `feature/onboarding/` + `Route.kt` + `KitNavHost.kt` + `SettingsRepository.kt` + `values/strings.xml`). Two triggers: `/kit-design-app` Phase 1B.5 (AskUserQuestion "Simple 3-page intro" vs "Personalised questionnaire") and standalone `/kit-design-onboarding`. **TaskCreate baked into 3 commands** for deterministic progress tracking: `/kit-start-setup` (9 tasks), `/kit-design-app` (5 + per-screen Phase 2), `/kit-upload-on-google-play` (1 + path-specific A–I or A–G). Skipped steps marked `completed` with `[skipped] ` title prefix. **Other polish:** `/kit-change-app-id` lost the "Confirm? Run rename?" prompt + git-status pre-check (steps renumbered 1→5); `/kit-start-setup` Step 0 feature list dropped the demo bullet and added Localization + Release; `/kit-start-setup` Step 2 (Onboarding content) stopped mentioning Urdu so Claude doesn't narrate "Urdu was removed" during setup; Stub kept as internal default fallback only. `compileDebugKotlin` green throughout. **Next: user device-tests RC paywall + new auth default + no-splash flow.**
- **2026-05-30 (demo nuke + cleanup pass)**: Runtime testing with a non-coder (teacher) revealed the demo broke no-auth/no-paywall promises — tapping "Launch demo" on Home jumped into a sealed app with hardcoded auth. **Nuked the demo entirely**: deleted `feature/demo/`, `Route.Demo`, `demoModule`, `SAMPLE_FEATURE_ENABLED`, habit_* strings, `/kit-remove-demo` command, `kit-start-setup` "Make it yours" step. `HomeScreen` rebuilt: brand mark + heading + **System status** panel (Auth / Paywall / Analytics / Remote config) reading live state from `AuthRepository.sessionState` + `PurchaseManager.isPremium` + `KitConfig` so a vibe-coder sees their setup choices reflected without a separate demo. Other cleanups same day: **Fastlane nuked** (`fastlane/` + `Gemfile` + 4 `.gitignore` lines) — `/kit-upload-on-google-play` is manual Play Console upload only, assets at `playstore/`. **Urdu strings nuked** (kit ships English-only by default; `/kit-translate` adds locales on demand with 4-bucket multi-select). Settings Language row auto-hides for single-locale builds. Settings Account section gated by `AUTH_ENABLED` (sign out / delete were leaking through Stub auth). `.debug` `applicationIdSuffix` removed (Firebase package-mismatch). Built `aso-googleplay-listing` skill at `.claude/skills/`. Built `/kit-design-app` (two-phase design + wire). Built `/kit-translate`. Built `/kit-upload-on-google-play` (no Fastlane, manual upload, screenshots via `aso-appstore-screenshots` or user-provided, listing via the new `aso-googleplay-listing` skill or write-own). Built `/kit-compile-app` + `/kit-run-app` runtime commands (auto-rewritten by `/kit-change-app-id`). Renamed every setup command to `kit-*` with verbal action names (kit-start-setup, kit-change-app-id, kit-setup-theme, kit-setup-auth, kit-setup-firebase, kit-setup-paywall, kit-setup-analytics, kit-setup-updates). Compile green across the lot. **Next: user device-tests the post-nuke flow.**
- **2026-05-23 (setup commands hardening + Supabase RemoteAppConfig + new default flow)**: Iterated on `/start-kit` and sub-commands from runtime testing — corrected Supabase UI (Project ID + Legacy anon tab), PostHog UI (Project token under Settings → General), made Claude place `google-services.json` from the dev's path, removed deep links / App Links from setup-ops (out of scope), dropped "Step N —" prefixes in setup-ops to stop nested-numbering collisions with `/start-kit`, hard-skipped paywall on "free app" choice. Built **Supabase as a `RemoteAppConfig` provider** modelled on the MyDiary `app_config` table: `SupabaseRemoteAppConfig` reads latest row by `updated_at`; `supabaseClientModule` extracted in DI so the same `SupabaseClient` serves auth and remote-config (Postgrest plugin installed alongside Auth); added `supabase-postgrest` to the catalog. Built the **default app flow** end-to-end: `Splash` (1.5s) → `Onboarding` (first launch only, DataStore `onboardingDone`) → `Auth` (if `AUTH_ENABLED` + signed out) → `Paywall` (if `PAYWALL_ENABLED` + not premium + first time, DataStore `paywallSeen`) → `Home` → `Settings` / `Profile` / `Changelog`. New screens: `SplashScreen`, `HomeScreen` (replaces `WelcomeScreen`), `ProfileScreen`. `CustomTabs` util. `KitConfig.PRIVACY_URL` + `KitConfig.TERMS_URL` (Chrome Custom Tabs). Sign-out from any post-auth screen routes back to Auth via `currentDestination.hasRoute<...>()` checks. Removed dynamic color from the kit entirely (`Theme.kt`). Added per-method sign-in toggles (`EMAIL_SIGN_IN_ENABLED` + `GOOGLE_SIGN_IN_ENABLED`) wired into `AuthScreen` conditional rendering. Added debug SHA-1 step to Google sign-in for both Supabase + Firebase paths via `./gradlew signingReport`. `compileDebugKotlin` green throughout. **Next: address bugs from device test (which surfaced the demo-confuses-non-coders issue).**
- **2026-05-22 (loose-ends + ops/permissions)**: Fixed code loose-ends (`AUTH_PROVIDER`→`STUB`, Retrofit baseUrl→`KitConfig.API_BASE_URL`, Room `dropAllTables` overload). Built backend-driven ops + features: maintenance mode + backend changelog — `UpdateManager` +`maintenance`/`latestVersionName`, new `ChangelogManager` parses `app_changelog` JSON from `RemoteAppConfig`. `UpdateGate` rewritten: maintenance screen → force update sheet → soft update sheet → auto "What's New" sheet (DataStore `lastSeenVersionCode`, fresh install suppressed). New components `KitUpdateSheet`/`KitWhatsNewSheet`/`KitMaintenanceScreen` (`designsystem/ops/`), `ChangelogScreen` (`feature/changelog/`, wired Settings → About → What's new + `DemoRoute.Changelog`). Permission priming: `KitPermission` enum + `rememberKitPermissionState` helper + `KitPermissionPrimer`/`KitPermissionPrimerSheet` (components+helper only). Removed dynamic app icons (rejected). `KitIcons` +4 roles (update/maintenance/camera/location) across 3 packs. Strings en+ur. `:app:compileDebugKotlin` green; detekt NOT run (user skipped). **Next: Phase 7.**
- **2026-05-22**: No code changes — planned sales/distribution/docs strategy with user (first time selling code). Decisions: (1) Updates = tagged releases + per-release changelog/migration-guide; git-remote-merge documented as optional. (2) Piracy = sell private-repo access + per-buyer watermark + LICENSE; real moat is updates/community/masterclass, accept some leakage. (3) Docs = per-page gating — public pages SEO-indexed, `gated:true` pages require buyer session + `noindex`. Phase 7 = ONE Next.js+Nextra site (landing + docs), auth-first checkout (GitHub OAuth → Merchant-of-Record payment → webhook adds repo collaborator), separate web repo. Cohorts stay on WhatsApp (Pakistan audience). Full plan saved to `.session/phase7-sales-and-docs-plan.md`. **User now device-testing Phases 0–6 before any further code. Resume: fix code loose-ends from test findings, then Phase 7.**
- **2026-05-18**: Discovery + planning. Studied shipfa.st, swiftstarterkits, flutterfasttemplate, shipkaro.dev, 1dayapp, mobile-docs. Locked scope: pure Android native, core 4 modules + Play compliance + Conversion + Ops packs, kit + landing page. Wrote this CLAUDE.md. Evaluated cortinico/kotlin-android-template vs Drjacky/MVVMTemplate → rejected multi-module for beginner/AI audience. Architecture locked: single `app` module package-by-feature, Koin DI, Retrofit/Room/DataStore.
- **2026-05-18 (cont.)**: Built Phase 0. Hand-scaffolded full source/config (build files, Koin graph, Material3 theme, type-safe nav, KitConfig/RemoteAppConfig, Room, DataStore, locale en+ur, 6 feature screens). Hit wrapper wall (can't author binary gradle-wrapper.jar; system Gradle 8.5 < AGP-required 8.7). Resolved by cloning cortinico template and overlaying its working wrapper (Gradle 8.14.5). `:app:assembleDebug` → **BUILD SUCCESSFUL**. Private repo created + pushed (github.com/wajahatkarim3/shipkaro-android-kit).
- **2026-05-18 (discussion)**: Locked demo/component model. Rejected `:demo` module (breaks single-module). Chosen: single-module, clean placeholder Home + dev-only debug-gated "View sample app" button → self-contained `feature/demo/` subtree reusing real components/infra with fake data. Reshaped Phase 3 (real component library, Swift-catalog parity) + Phase 6 (in-app demo showcase) + Phase 8 (catalog docs, removal guide). Permissions + screenshots deferred to Phase 8.
- **2026-05-18 (reorder)**: User flagged building auth before components = double work. Reordered: Phase 1 = Component Library + Design System, Phase 2 = Auth, Phase 3 = Monetization. Icons decision changed to compose-icons Gradle lib (feather+tabler default; composables.com in docs for Heroicons). **Next: Phase 1 (components, design system, icons).**
- **2026-05-20**: Built Phase 1. KEPT `material-icons-extended` (user reversed earlier drop decision). Designed pluggable icon system: `KitIcons` interface + 3 packs (Material default / Feather / Tabler) + `LocalKitIcons` CompositionLocal + per-callsite `ImageVector?` override. User extends by subclassing one of the `*Impl` open classes. Built foundation tokens (Spacing/Shape/Elevation), expanded color scheme (surfaceVariant/outline/error), full M3 typography. Wrote 10 core components, 5 state views, onboarding pager, 7 settings rows. Skipped dynamic app icon (deferred). `:app:assembleDebug` BUILD SUCCESSFUL. **Next: Phase 2 (Auth + Account on Phase 1 components).**
- **2026-05-21 (Phase 6)**: Built demo + design pass, split 6a/6b. **6a:** rebranded to shipkaro purple (`Color.kt`, `BrandPrimary #7C3AED`, dynamicColor off), `ic_shipkaro_mark` logo vector, professional redesign of Onboarding/Paywall/Auth/Settings to Swift-starter-kit grade (user supplied logo + reference shots, asked for pro design via ui-ux-pro-max skill). New components: KitPricingCard, KitFeatureRow, KitBulletRow; settings icon-chips; KitIcons +4. **6b:** demo model REVISED mid-phase (user: kit has no real flow out of box — just Welcome + Demo button). Built `WelcomeScreen` (kit-entry placeholder), `feature/demo/` self-contained subtree (DemoNavHost splash→onboarding→paywall→auth→habits→settings, Habit Tracker on demo-owned Room, free 3-habit cap → paywall). Stripped `KitNavHost` to `Welcome→Demo`, deleted Phase 0 `feature/home` + `feature/sample`. Demo reuses real auth/onboarding/paywall/settings screens. `:app:assembleDebug` + `:app:detekt` green. Runtime not yet device-tested. **Next: Phase 7 (Landing/Sales page) — or device-test Phase 6 first.**
- **2026-05-21 (Phase 5)**: Built Launch Tooling. Wired detekt to `:app` (Phase 0 deferred item) — autoCorrect fixed formatting kit-wide, config tuned for Compose (LongMethod 150, CyclomaticComplexMethod 25, LongParameterList ignoreAnnotated Composable, MatchingDeclarationName/ForbiddenComment off — kit deliberately groups component+enum per file + ships TODO author-guidance). Renamed `Routes.kt`→`Route.kt`. Ported KMM starter's `refactorPackage` Gradle task → adapted single-module native (detects `namespace`, renames `.kt` package/import + `namespace`/`applicationId` + `app_name` per-locale + `src/{main,test,androidTest}/java` dirs). Fastlane scaffold (`Fastfile` internal/promote/metadata/screenshots lanes, `Appfile`, `Gemfile`, `metadata/android/en-US/` listing copy). `release.yml` GitHub Actions workflow (signed AAB via `RELEASE_*` env + optional Play internal upload); `release` buildType signingConfig guarded — unsigned locally, signed in CI. `legal/` templates (privacy, terms, Play Data Safety per-SDK mapping). `CHANGELOG.md` seed. ASO/marketing NOT copied into kit per user — `fastlane/metadata` doubles as listing template, full playbook stays on docs site. Reference docs: `shipkaro-1dayapp-docs` (Docusaurus — marketing/aso, tools, deployment, guides/refactor-package). `:app:assembleDebug` + `:app:detekt` green. **Next: Phase 6 (In-App Demo Showcase).**
- **2026-05-21 (Phase 4 complete)**: Finished Ops. `FirebaseRemoteAppConfig` impl of `RemoteAppConfig` (generation-counter flow so collectors re-read after fetchAndActivate); `KitConfig.REMOTE_CONFIG_PROVIDER` LOCAL default. FCM: `KitMessagingService` + `KitNotifications` channel + POST_NOTIFICATIONS perm + manifest service & https App Links intent-filter (`shipkaro.dev` placeholder, autoVerify). Update gate: `UpdateManager` compares `BuildConfig.VERSION_CODE` against `min_supported_version`/`latest_version` remote keys → `UpdateGate` composable wraps nav host, REQUIRED = non-dismissible KitDialog, OPTIONAL = dismissible. In-app review: ported KMM `InAppReviewManager` (Play API, `FakeReviewManager` in debug), wired to Settings → About → Rate row. `logScreen` threaded into Auth/Paywall/Settings (Phase 0 placeholders skipped — they get replaced). All Firebase ops pieces inert until google-services.json added. `play-review` dep added (no KMP). `:app:assembleDebug` green. **Next: Phase 5 (Launch Tooling).**
- **2026-05-21 (Phase 4, partial)**: Started Analytics + Ops. Ported KMM starter's analytics layer — KMM had `Analytics` (expect/actual bridge) + `AnalyticsManager` (gated facade); native Android collapses to one `AnalyticsManager` class fanning out to PostHog (`com.posthog:posthog-android:3.44.2`) + Firebase Analytics + Crashlytics. Analytics user-toggle persisted in DataStore (`SettingsRepository.analyticsEnabled`), surfaced as a ToggleRow in Settings → Privacy section; crash reporting (`logError`) intentionally NOT gated by the toggle. PostHog key from `local.properties` (`posthog.api.key`/`posthog.host`) → BuildConfig. Firebase Analytics/Crashlytics inert until google-services.json + plugins added. `AnalyticsEvents`/`AnalyticsParams`/`ScreenNames` constants. `:app:assembleDebug` green. **Phase 4 remaining: remote config/feature flags, FCM push + deep links, update gate, in-app review.**
- **2026-05-21 (Phase 3)**: Built Monetization. Studied the ShipKaro KMM starter (`../shipkaro-kmm-starter`) per user — ported its `PurchaseManager` manager-pattern to native (`com.revenuecat.purchases:purchases:9.23.1`, no KMP). KMM used RC's prebuilt `Paywall` composable; kit diverges — custom `PaywallScreen` on Phase 1 components (hard + soft variants per CLAUDE.md), `purchases-ui` excluded to avoid Compose-Multiplatform dep conflict (same class as Phase 2's compose-auth-ui). PurchaseManager extended beyond KMM's (which only did refresh+restore) with `offerings` + `purchase()` since custom paywall owns purchasing. KitConfig: `ENTITLEMENT_ID` + `PAYWALL_MODE`; RevenueCat key in `local.properties` (`revenuecat.android.api.key`) → BuildConfig. `BillingErrorCode` enum + localized mapping. RevenueCat configured in KitApplication.onCreate. KMM starter also has an AnalyticsManager — noted for Phase 4. `:app:assembleDebug` green. **Next: Phase 4 (Analytics + Ops).**
- **2026-05-21**: Built Phase 2. Verified supabase-kt is the only first-party Android Supabase client (KMP under hood; quickstart user provided uses it); user picked supabase-kt with `ktor-client-okhttp` engine to share OkHttp with Retrofit. Hit toolchain wall — supabase-kt 3.6.0 transitives require AGP 8.9+ / Kotlin 2.2+ / compileSdk 36. Upgraded toolchain comprehensively (see Phase 2 checklist). Dropped `compose-auth` + `compose-auth-ui` artifacts — they drag Compose Multiplatform material3 (1.9.0-beta) + activity 1.12.x which conflict with androidx Material3. Designed provider-agnostic AuthRepository + 3 impls (Stub default, Supabase, Firebase). KitConfig.AUTH_PROVIDER picks via Koin. Google sign-in: Credential Manager primary (native bottomsheet) → falls back to OAuth deeplink (Custom Tabs + supabase.handleDeeplinks). KitConfig.GOOGLE_WEB_CLIENT_ID + local.properties SUPABASE_URL/KEY → BuildConfig. Account deletion + DataStore + Room wipe. User flagged hardcoded "Continue with Google" → externalised ALL Phase 2 strings to res/values/strings.xml + values-ur/strings.xml (en+ur); KitDialog dismissLabel made required (no English default); KitBanner contentDescription + KitPasswordField show/hide a11y strings localized. Added i18n rule to Decisions + saved memory. `:app:assembleDebug` BUILD SUCCESSFUL. **Next: Phase 3 (Monetization — RevenueCat).**

## Important Decisions Made
- **Pure Android native, not KMP** — Cohort 1 ran KMP; user found it a bad decision. iOS native deferred to a future separate effort.
- **Auth is dual**: Supabase primary, Firebase Auth toggle-able — broadens standalone-buyer appeal.
- **Single-module, package-by-feature** — multi-module rejected: target audience = Android beginners + AI vibe-coding + weekend ship; build-logic/convention-plugin indirection hurts them, multi-module payoff irrelevant at indie scale.
- **Base = cortinico template, app module only** — its library-android/kotlin/compose modules are library-publishing demos, not needed; keep tooling (version catalog, detekt/ktlint, CI).
- **DI = Koin** — no Hilt/KSP; less magic, faster builds, simpler for beginners + AI.
- **Patterns from MVVMTemplate, flattened** — MVVM + NetworkResponse + Material3/edge-to-edge; drop build-logic, RxJava, KSP.
- **DEVIATION: Navigation Compose 2.8 type-safe instead of androidx Navigation 3** — Nav3 still alpha, unsafe for a stability-critical beginner kit. Same type-safe-route goal delivered via kotlinx.serialization routes. Revisit when Nav3 stable.
- **Wrapper bootstrapped from cortinico template** — gradle-wrapper.jar is binary, not authorable. Cloned template, copied its Gradle 8.14.5 wrapper. Toolchain: **AGP 8.9.2, Kotlin 2.2.21, KSP 2.2.21-2.0.4, Room 2.7.2, JDK 17, compileSdk 36, targetSdk 35, minSdk 26, Compose BOM 2025.06.00.** Bumped from Phase 0's (8.5.2 / 2.0.21 / 34 / 2024.09.02) in Phase 2 to satisfy supabase-kt 3.6.0 transitive deps (androidx.browser 1.10.0 + ktor 3.4.x metadata).
- **Two-layer config** — `KitConfig` (compile-time, kit author edits) vs `RemoteAppConfig` (runtime, end-app wires to Supabase/Firebase). Kept strictly separate per user.
- **Net/persistence = Retrofit + Room + DataStore** — max tutorial/AI coverage (chose over old kit's Ktor for learnability).
- **Monetization = RevenueCat native SDK** (`com.revenuecat.purchases:purchases`) — `PurchaseManager` singleton manager-pattern ported from the ShipKaro KMM starter. Custom paywall on kit components (NOT RC's prebuilt `purchases-ui` — drags Compose-Multiplatform deps + needs hard/soft variants). API key in `local.properties`, entitlement id in `KitConfig`.
- **Sold standalone + free for cohort** — ShipFast pricing model (one-time, lifetime updates).
- **Checklist lives here in CLAUDE.md** (survives /compact), not in task tool — per user's stated workflow.
- **Demo = in-app, single-module, NOT a module/flavor/branch** — `:demo` Gradle module rejected (forces multi-module). Self-contained `feature/demo/` subtree.
- **DEMO MODEL REVISED (Phase 6)** — earlier plan: clean placeholder Home as attendee's real start + debug-gated demo button. REPLACED: kit has NO real app flow out of the box. `KitNavHost` = `Welcome → Demo` only. `WelcomeScreen` (placeholder, dev replaces) shows logo + kit info + "Launch Demo" button gated by `SAMPLE_FEATURE_ENABLED` (no `BuildConfig.DEBUG` gate — demo IS the out-of-box experience). The demo (`feature/demo/`) runs the full micro-SaaS flow: splash → onboarding → paywall → auth → habit tracker → settings. The infra screens (onboarding/auth/paywall/settings) are NOT wired to any real flow — they exist as reusable code, consumed only by the demo. Dev builds their app by: replacing WelcomeScreen, deleting `feature/demo/`, re-wiring `KitNavHost`.
- **Demo uses REAL auth + REAL data, not fake** — reuses the real `AuthRepository` (STUB default; Supabase if configured). Habit data persists in a demo-OWNED Room DB (`feature/demo/data/DemoDatabase`, separate from `KitDatabase`) so deleting `feature/demo/` stays clean. Only "demo-mode" bit: paywall placeholder when RevenueCat unconfigured + seeded sample habits.
- **Demo app = Habit Tracker** — list/streak/done-today, free 3-habit cap → paywall. Chosen over Expense/Notes/Quotes: cleanest premium gate + recurring-use loop, Room-only (no fake API).
- **Component library, no in-app showcase** — Swift-catalog parity via well-organized `designsystem/` packages; documented in /docs, not a gallery screen. Infra screens built AS reusable components.
- **Phase 0 screens are throwaway tech debt** — replaced by real components across Phase 1–3/6.
- **Phases reordered (components first)** — Phase 1 = component library, then Phase 2 auth, Phase 3 monetization. Screens consume components; building screens first = double work (rejected, per user).
- **Icons = pluggable pack system, Material default** — `KitIcons` semantic interface + 3 bundled impls: `MaterialKitIcons` (default, material-icons-extended), `FeatherKitIcons`, `TablerKitIcons`. `LocalKitIcons` CompositionLocal lets `ShipKaroTheme(icons = …)` swap kit-wide in one line. Components consume via `KitTheme.icons.back` etc. User extends by subclassing `MaterialKitIconsImpl()` to override individual icons, or writes a custom `KitIcons` impl. Per-callsite override = any component taking an icon accepts an `ImageVector?`. `material-icons-extended` KEPT (reversed earlier drop). compose-icons feather+tabler shipped; others commented opt-in in catalog. R8 strips unused.
- **Dynamic app icons — REJECTED** (not deferred). User: useless feature. No runtime launcher-icon switching in the kit; not building it.
- **Ops are backend-driven via `RemoteAppConfig`** — update gate (force/soft), maintenance mode, and the changelog all read conventional keys (`min_supported_version`, `latest_version`, `maintenance_mode`, `app_changelog` JSON, …) from `RemoteAppConfig`, so they work with whatever provider is wired (LOCAL/Firebase; Supabase provider is a future option). No static changelog file — `ChangelogManager` ships a `SAMPLE_CHANGELOG` default so the screen is demoable offline.
- **ALL user-facing strings MUST be localized** — never hardcode UI text. Every screen / VM-status / component contentDescription / dialog label / accessibility hint goes into `app/src/main/res/values/strings.xml`. **Kit ships English-only by default** (revised 2026-05-30 — Urdu bundle was removed because the dev never asked for it); additional locales are added on demand via `/kit-translate`. Components should accept String params (not hardcode English defaults) so callers stay localizable; where a sensible English fallback is unavoidable, source it via `stringResource()` so future translations work. Audit before commit: grep for hardcoded Western-script literals inside `Text(...)`, `contentDescription = "..."`, `label = "..."`, `confirmLabel = "..."`. Tests + reviewers should reject hardcoded strings.
- **DEMO NUKED (2026-05-30)** — overrides the earlier "Demo model revised" decision. Runtime testing with a non-coder showed the bundled demo was a sealed second app with its own auth, contradicting the dev's no-auth choice when they tapped "Launch demo" on Home. The kit's real flow (Splash → Onboarding → Auth → Paywall → Home) now wires every feature end-to-end, so the demo is redundant + actively confusing. Removed the whole `feature/demo/` subtree, `Route.Demo`, `demoModule`, `SAMPLE_FEATURE_ENABLED`, `habit_*` strings, and the `/kit-remove-demo` command. `HomeScreen` is now a brand-mark + heading + **System status** panel reading live `AuthRepository.sessionState` / `PurchaseManager.isPremium` / `KitConfig` values, so a non-coder sees their setup choices reflected on screen.
- **Default app flow** — `Splash` (1.5s) → `Onboarding` (first launch only) → `Auth` (if `AUTH_ENABLED` + signed out) → `Paywall` (if `PAYWALL_ENABLED` + not premium + first time) → `Home` → `Settings` / `Profile` / `Changelog`. Each gate skipped per config + DataStore-tracked first-launch flags (`onboardingDone`, `paywallSeen`). Sign-out from post-auth bounces to Auth via `currentDestination.hasRoute<...>()` checks in `KitNavHost`. Replace `HomeScreen` with your app's main screen.
- **Setup commands = vibe-coder UX** — every setup command lives under `.claude/commands/` with the `kit-` prefix so devs type `/kit-` to discover everything. Verbatim `> ...` blocks in command files MUST be shown to the developer as-is (no paraphrasing); prose outside those blocks is instructions to Claude. `/kit-start-setup` runs sub-commands inline; sub-commands' verify steps are skipped inside the orchestrator and one build runs at the end via `/kit-run-app`. `/kit-change-app-id` rewrites `/kit-run-app.md` so it always launches the dev's renamed app.
- **Per-method sign-in toggles** — `KitConfig.EMAIL_SIGN_IN_ENABLED` + `KitConfig.GOOGLE_SIGN_IN_ENABLED` gate the email form and the Google button in `AuthScreen` independently. `/kit-setup-auth` asks which (Email + Google / Email only / Google only) for Supabase + Firebase providers; STUB still shows the auth screen so dev sees the flow.
- **Dynamic color removed entirely** — `ShipKaroTheme` no longer has a `dynamicColor` parameter; the app always uses the brand-derived light/dark scheme so branding is consistent. Audience preference: "Don't overwhelm with optional toggles".
- **Fastlane removed** — `/kit-upload-on-google-play` does manual Play Console upload only. Release assets land at `playstore/` (title.txt, short_description.txt, full_description.txt, screenshots/, changelogs/<versionCode>.txt). The Fastlane gem + dir + `.gitignore` rules deleted 2026-05-30.
- **Privacy / Terms URLs** — `KitConfig.PRIVACY_URL` + `KitConfig.TERMS_URL` (placeholders). Settings → Privacy / Terms opens them in Chrome Custom Tabs (`core/util/CustomTabs.kt`). Collected in `/kit-upload-on-google-play` pre-flight (release-time, not dev-env setup).
- **Supabase RemoteAppConfig** — `SupabaseRemoteAppConfig` reads a public `app_config` Postgres table (kit-aligned columns: `min_supported_version`, `latest_version`, `latest_version_name`, `maintenance_mode`, `maintenance_message`, `app_changelog`, `updated_at`). Selected via `KitConfig.RemoteConfigProvider.SUPABASE`. Reuses `supabase.url` / `supabase.key`, so apps already on Supabase auth need no extra credentials. `supabaseClientModule` in DI installs Auth + Postgrest plugins so one client serves both. Modelled on MyDiary's `AppConfigManager`.
- **Setup commands prefixed `kit-*` with verbal action names** — non-coder-friendly: `/kit-start-setup`, `/kit-change-app-id` (not "refactor"), `/kit-setup-theme`, `/kit-setup-auth`, `/kit-setup-firebase`, `/kit-setup-paywall`, `/kit-setup-analytics`, `/kit-setup-updates` (was setup-ops), `/kit-compile-app`, `/kit-run-app`, `/kit-translate`, `/kit-upload-on-google-play`, `/kit-design-app`. Plus the `aso-googleplay-listing` skill.
- **No deep links / App Links setup** — out of scope for micro-SaaS apps the kit targets. Removed from `/kit-setup-updates`.
- **HomeScreen no demo button** — replaced with a System Status panel reading auth state, paywall mode, analytics flag, and remote-config provider. Non-coder can confirm setup choices took effect; nothing to mis-click into.
- **Discuss-before-edit (collaboration rule)** — when the user asks a question or is mid-discussion, answer first and do NOT edit/commit/push until explicit go-ahead. A standing "make the changes and commit" instruction does not override an open sub-decision.
- **Don't-overwhelm-beginners (collaboration rule)** — prefer sensible defaults over questions; cut optional toggles + long click-paths. Drove: dynamic-color removal, app-icon step moved from setup to release, deep links removed from ops, listing-copy + screenshot skills offered as one-click options.
- **Terminal-first cohort workflow (deferred)** — cohort attendees build/run/deploy via CLI (Android Studio only for SDK install); `android` CLI + Android Skills + scrcpy. Memory saved; setup-flow branching ("AS available / terminal only") planned but not yet built. See `shipkaro.dev/mobile-docs/getting-started/setup-macos`.
- **PAYWALL = RC PREBUILT (revised 2026-05-31)** — overrides the earlier "Custom paywall on kit components" decision. The kit now renders RevenueCat's `Paywall` composable from `com.revenuecat.purchases:purchases-ui:9.23.1`. That artifact IS standard Jetpack Compose for Android (not Compose Multiplatform — the earlier CMP-conflict note was incorrect). `PaywallScreen.kt` wraps it with a `PaywallListener` forwarding `onPurchaseCompleted` (and `onRestoreCompleted` with active entitlements) to the nav callback. The paywall design + copy + offerings + price strings live in the RC dashboard; the kit owns no paywall UI code. `PurchaseViewModel.kt` deleted (RC manages state internally). `PurchaseManager` kept for the `isPremium` `StateFlow` used by the nav resolver. If RC is unconfigured, the composable renders RC's own error state on device — acceptable during dev.
- **AUTH default = OFF (revised 2026-05-31)** — `KitConfig.AUTH_ENABLED` ships `false`. A fresh clone runs Splash (OS) → Onboarding → Home with no auth screen. `/kit-setup-auth` flips `AUTH_ENABLED = true` AND sets `AUTH_PROVIDER` to a real backend in the same step. **Stub is no longer offered** as a `/kit-setup-auth` choice — it had no production purpose. The enum value + `StubAuthRepository` remain as an internal fallback for the edge case where `AUTH_ENABLED` is true but no real provider has been picked.
- **NO IN-APP SPLASH (revised 2026-05-31)** — the kit's `SplashScreen.kt` + `Route.Splash` are gone. Android's OS cold-start splash is the only splash. `KitNavHost` resolves the start route in a `LaunchedEffect(Unit)` during initial composition and renders a blank `Box` with the theme background while resolving, so the OS splash stays visible. Once resolved, NavHost composes with `startDestination = <resolved route>`. Reason: runtime testing showed users see two splashes (OS + ours) and the second is jarring.
- **Theme = single-knob brand color drives dark + containers (revised 2026-05-31)** — `/kit-setup-theme` Step 1 now derives `LightPrimaryContainer`, `LightOnPrimaryContainer`, `DarkPrimary`, `DarkOnPrimary`, `DarkPrimaryContainer`, and `DarkOnPrimaryContainer` from `BrandPrimary` via HSL targets (hue preserved, lightness/saturation per Material 3-ish tone targets). Previously only `LightPrimary` derived from the brand; dark stayed hardcoded purple.
- **Icon pack picker** — `/kit-setup-theme` Step 2 lets the dev choose Material (default) / Feather / Tabler / "Pick another" (Simple Icons / Font Awesome / Eva / Octicons / Line Awesome / Linea / Weather / CSS GG). Each option's description carries the pack's preview URL. "Apply" step disables unused packs **reversibly** — comments the catalog line + the `implementation(...)` line + wraps the impl file content in `/* ... */`. Switching packs later = uncomment three places.
- **Onboarding questionnaire skill** — `.claude/skills/onboarding-questionnaire/SKILL.md`. Compose-native Calm/Headspace-style 10–14 screen flow generator (Welcome → Pain → Solution → Quiz intro → 3–5 question screens → Social proof → Personalised plan → Permission priming → Auth nudge → Ready). Hard scope: only `feature/onboarding/`, `Route.kt`, `KitNavHost.kt`, `SettingsRepository.kt`, `values/strings.xml`. Two triggers: `/kit-design-app` Phase 1B.5 ("Simple 3-page intro" vs "Personalised questionnaire") and standalone `/kit-design-onboarding`.
- **Deterministic step tracking** — `TaskCreate` baked into `/kit-start-setup` (9 tasks), `/kit-design-app` (5 + per-screen Phase 2 tasks added after screen list locks), `/kit-upload-on-google-play` (1 + path-specific A–I first-version OR A–G update). Skipped steps marked `completed` with `[skipped] ` title prefix via `TaskUpdate`.
- **No confirmation prompt on rename** — `/kit-change-app-id` runs the Gradle task immediately after validating `newAppId`. Removed the "Confirm? Run rename?" prompt + the `git status` pre-warning. Steps renumbered to 5.
- **Logging = Timber + CrashlyticsTree (added 2026-06-01)** — kit ships `com.jakewharton.timber:timber:5.0.1`. `KitApplication.onCreate` plants `Timber.DebugTree()` in debug and a custom `CrashlyticsTree` (in `core/log/`) in release, gated on `KitConfig.ANALYTICS_ENABLED`. INFO+ becomes a Crashlytics breadcrumb (`Crashlytics.log()`); ERROR/ASSERT becomes a non-fatal (`recordException()`). Devs add new `Timber.Tree` impls (Sentry, file, etc.) by planting them alongside; the kit's own code uses `Timber.tag(...).w(throwable, ...)` instead of `android.util.Log`.
- **Rate button = Play Store listing, NOT in-app review (added 2026-06-01)** — Settings → Rate calls `PlayStoreLauncher.openListing()` (`market://details?id=<applicationId>` + web fallback). Reason: Play's in-app review API is quota-throttled and silent-no-ops at most call-sites, making "tap Rate, nothing happens" the common case. The in-app review dialog now lives behind `/kit-setup-review-dialog`, where the dev picks a trigger that fits the app (Nth launch / key action / time delay / manual). `InAppReviewManager` + its Koin binding stayed; only the Settings call-site moved.
- **SecureDataStore = AES-GCM + Android Keystore (added 2026-06-01)** — `core/security/SecureDataStore.kt`. Master key in `AndroidKeyStore` provider (`KeyGenParameterSpec` `BLOCK_MODE_GCM` + `ENCRYPTION_PADDING_NONE` + `setRandomizedEncryptionRequired(true)` + 256-bit). Ciphertext + 12-byte IV packed + Base64 string, persisted in a dedicated DataStore Preferences file `kit_secure_prefs`. Single Koin singleton. **No new deps** — `javax.crypto` + AndroidX Keystore APIs only. **Skipped `EncryptedSharedPreferences`** — AndroidX deprecated it Jul 2024. Scope = dev-held secrets (user-entered API keys, third-party OAuth tokens). The kit's own auth-token storage stays inside Supabase / Firebase / RevenueCat SDKs, which already encrypt at rest.
- **AI integration = OpenRouter, provider-specific class names (added 2026-06-01)** — `core/ai/OpenRouterAiClient` + `OpenRouterAiRepository` + `OpenRouterAuthInterceptor`. **No `AiClient` interface** — premature abstraction; a future second provider (`TogetherAiRepository`, `GroqAiRepository`, `OpenAiRepository`) gets its own class. Method names are use-case-generic (`generateText`, `generateTextWithMessages`, `streamText`, `generateJson`) and **model-first** — the model parameter changes most often per call (cheap for parsing vs premium for generation). **No "chat" naming**, no sample chat screen — plumbing only. OpenRouter Retrofit is a separate named singleton `retrofit_openrouter` with base URL `https://openrouter.ai/api/v1/`, fully isolated from the app's own `KitConfig.API_BASE_URL` Retrofit (so devs adding their own backend never collide). `KitConfig.OPENROUTER_ENABLED` (default false) + `OPENROUTER_DEFAULT_MODEL` (default `meta-llama/llama-3.2-3b-instruct:free`). Key in `local.properties` → BuildConfig.
- **Analytics commands split: SETUP vs PLAN-RELEASE (added 2026-06-01)** — `/kit-setup-analytics` stays SDK-plumbing (one-time, in `/kit-start-setup`). New `/kit-plan-release-analytics` is the per-release strategy step: scans changelog + screens + existing events, derives release-goal options, suggests 3–5 funnel events, **auto-inserts** `analytics.logEvent(...)` at Grep'd call-sites (heuristic by event-name shape). Ambiguous matches (0 or 3+) confirm with the dev before editing. Wired into `/kit-upload-on-google-play`. Hybrid auto-insert because the kit's audience is non-coder — snippet handouts don't work for that audience.
- **Privacy policy = codebase-driven generator, ToS = template (added 2026-06-01)** — `/kit-generate-legal` scans `libs.versions.toml` + `KitConfig` flags + Retrofit `@GET/@POST/@PUT/@DELETE/@PATCH` annotations + Supabase `client.from()` calls, asks 8 legal Qs, and writes `playstore/privacy_policy.md` + `playstore/privacy_policy.html` (self-contained styled page; host on GitHub Pages / Vercel / Netlify for the Play-required public URL) + filled `playstore/play_data_safety.md` (per-SDK Play form answers). The old `legal/play-data-safety.md` stays as a template. **ToS stays template** (`legal/terms-template.md`) — no codebase signal makes it dev-specific; pure boilerplate. Phase 7 landing page will MDX-render the `.md`.
- **Phase 7 site = separate private repo, ShipKit brand, $29→$49 + cohort coupon (locked 2026-06-04)** — `shipkit-site` private GitHub repo at https://github.com/wajahatkarim3/shipkit-site (local path `../shipkit-site`). Next.js 14 app-router + Tailwind + TypeScript; no shadcn CLI dep (hand-rolled w/ Tailwind utility classes); no `create-next-app` (manually scaffolded for reproducibility). Domain target = `kit.shipkaro.dev` subdomain. Payment = LemonSqueezy MoR. Pricing = $29 early-bird first 30 buyers → $49 after; cohort attendees free via per-cohort 100%-off LemonSqueezy coupon (same checkout flow, same webhook, same GitHub auto-collaborator add). 14-day money-back. Community = WhatsApp only for now. Hero = autoplay screen recording of `/kit-start-setup` (user records). Comparison table uses generic "Other kits" label (no named competitors). Multi-platform teaser on landing: Android Native Available + iOS Native / Flutter / React Native / Web all "Coming soon" pills so buyers see the bigger picture (one kit per platform, future purchases). Day-by-day plan in `shipkit-site/README.md` + `.session/phase7-sales-and-docs-plan.md`.
- **Phase 7 site brand = Navy + Gold + Deep-ink (revised 2026-06-04, supersedes purple)** — initial scaffold used the Android kit's `#7C3AED` purple; same-day v1 repaint locked **Midnight Sky** palette: `#00296B` navy + `#FDC500` gold (CTAs/highlights only). v2 deep-ink pass (user: "too much blue") then layered `ink-950 #020617` page bg + `ink-900 #0A0F1F` surface bands + `ink-800 #111827` cards + `ink-700 #1F2937` hover on top. Page reads as **surface bands** instead of a continuous wall (Hero/TrustStrip/Features/TechStack/Comparison/FAQ/Footer = ink-950; PlatformStrip/ShowDontTell/Pricing/FinalCta = ink-900 breaks). **Gold-scarcity rule:** gold appears ONLY at CTAs (always), eyebrows, Hero accent word + glow, Comparison ShipKit ✓ marks + column tint, Pricing main-card border + `$29` price + corner glow, FinalCta accent word + corner glows, logo squares. Exactly **one gold accent word per section h2** (8 of 8). Typography = **Geist Sans + Geist Mono** via `next/font` (CSS vars `--font-geist-sans` / `--font-geist-mono`); Tailwind `sans/display/mono` all resolve to Geist; `tracking-tightest -0.04em`; hero h1 `text-6xl → text-9xl` @ 800 with `leading-[0.95]`. The Android kit's own `BrandPrimary #7C3AED` is unaffected — site and app no longer share a color.
- **Phase 7 site positioning = 3 pillars (locked 2026-06-04, vibe-coder rewrite)** — supersedes the earlier "pure-Android-native + cohort/community" line. Audience = vibe coders (use Claude Code / Cursor / Windsurf to build apps). Pillars: **(1) AI-agent ready** (slash commands like `/kit-start-setup` walk through provider setup; "AI handles everything, you ship"); **(2) End-to-end** (design → onboarding → auth → paywall → analytics → permissions → Play Store upload → privacy policy — all in the kit, all driven by commands); **(3) No Android Studio needed** (everything from the terminal — this is the #1 differentiator vibe coders feel most). **Locked hero headline: "Ship your Android app. Without Android Studio."** Sub: "Auth, payments, analytics, Play Store upload — all done by your AI agent from the terminal. Type /kit-start-setup, answer a few questions, ship by lunch." **Copy rule:** strip every nerd term that signals "I need to be an Android dev to use this" — no SHA-1, no Compose/Credential Manager/OAuth/Play App Signing, no KitConfig/Retrofit/Supabase Storage. Substitute concrete user-visible benefits ("the native sheet, not a browser tab" / "5 minutes, no keys to copy-paste" / "Pretty 'allow camera/photos/etc' screens, already built and styled"). **Trust signal in TrustStrip = author credentials, not adjectives:** "indie Android dev since 2014, 12+ published apps, 300+ devs in the community". **Pricing CTA = standard GitHub-style black button** with octocat SVG (recognition > novelty, reduces friction). FAQ opens with "I don't really know how to code. Can I still use ShipKit?" (the unspoken first concern) and includes "Do I need to open Android Studio?" (the #2 fear).
- **BRAND NAME = NowKit (renamed 2026-06-05, supersedes "ShipKit")** — "ShipKit" collides on Google/Play/npm (Docker BuildKit-adjacent, the old LaunchKit, several "ShipKit" starter kits). Web-scanned a long verb+kit list; the whole `[common-word]+Kit` namespace is taken or SEO-buried (ShipKit/BuildKit/LaunchKit/SoloKit/AppKit/MobileKit/FastKit/SpeedKit). Only **Quickit** and **NowKit** scanned clean; picked **NowKit** for crisp pronunciation + "ship now" energy. Renamed all brand display text across `shipkit-site` (commit `2b44638`); **"by shipkaro" attribution stays** (NowKit is the product, ShipKaro the parent). **Unchanged on purpose:** GitHub repo `shipkit-site`, Vercel `shipkit-site.vercel.app`, and all `/kit-*` slash commands (generic, not brand-bound). Avatar `shipkit-site/public/nowkit-avatar.png` (500×500, rendered from MetallicMark via headless Chrome). **Resolved 2026-06-06:** GitHub repo (`shipkit-site`) + Vercel project + Vercel URL (`shipkit-site.vercel.app`) deliberately KEEP the `shipkit` names — only the brand display name changed. Public domain = **`kit.shipkaro.dev`** (user connects it on Vercel themselves). `nowkit.dev` domain grab + trademark/Play name scan dropped (not pursued).
- **Build brand assets from the real component, not AI raster (2026-06-05)** — when the user needs a logo/avatar/OG image that already exists as a live component (e.g. `MetallicMark`), reproduce it from the actual CSS spec + real font (Geist-Variable) via headless-Chrome screenshot, NOT `mcp__gemini__generate_image`. AI raster drifts from the exact gradient/cursor/font and isn't pixel-faithful. Pattern: static HTML embedding the font via `file://` @font-face → `Google Chrome --headless --screenshot --force-device-scale-factor=2` → `sips -z` downscale. Generator kept at `shipkit-site/scripts/nowkit-avatar.html`.
- **Full-screen permission flows (added 2026-06-02)** — `core/designsystem/permission/KitPermissionScreen.kt` (parameterised) + `feature/permissions/PermissionScreens.kt` (8 one-liner wrappers — Notifications / Camera / Microphone / Photo / Contacts / Calendar / Location / Motion). Mirrors SwiftStarterKits' per-permission view-screens; ours wire the OS request through `rememberKitPermissionState`. The screen auto-swaps "Allow" → "Open Settings" + a denied-state rationale when the OS marks the permission permanently denied. iOS-only permissions (Tracking ATT, Music Library) deliberately omitted. Account-management screens (ChangePassword / ChangeEmail / VerifyPassword / VerifyEmail / ResetPassword) deliberately deferred — they need real backend hooks per provider and are easier to build when a concrete app drives them.
- **Setup-command pacing rule (locked 2026-06-02)** — every `/kit-setup-*` command MUST split provider walkthroughs (Supabase, Firebase, Google Cloud, RevenueCat, PostHog, Sentry) into discrete sub-steps with **STOP and wait** gates between them. Never chain providers in one message. Never run a shell command (signingReport, keytool, etc.) until the developer explicitly says "yes" / "ready" / "go". This rule was added because runtime testing with non-coders showed they lose place when Claude dumps a multi-provider block at once. Applies to `/kit-setup-auth` (Steps 4a/4b sub-steps), `/kit-setup-paywall` (Step 2 sub-steps), `/kit-setup-analytics` (Steps 2/3/4 with per-provider waits), `/kit-upload-on-google-play` (Step D sub-steps), and any future provider-walkthrough command.
- **Setup-command idempotency / resume (added 2026-06-02)** — every `/kit-setup-*` command opens with `Step 0 — Detect existing state`. Reads `KitConfig.kt` + `local.properties` + relevant files. Branches: fully configured → AskUserQuestion (keep-as-is / reconfigure / change one thing — with one-thing options that jump straight to a sub-step); partially configured → walks only the missing sub-steps after announcing what's being skipped; nothing configured → walks the full flow. Interrupted runs resume from where they left off. Per-command state: `/kit-setup-ai` (OPENROUTER_ENABLED + openrouter.api.key); `/kit-setup-analytics` (ANALYTICS_ENABLED, SENTRY_ENABLED, posthog.api.key, sentry.dsn, google-services.json + plugins); `/kit-setup-auth` (AUTH_ENABLED, AUTH_PROVIDER, EMAIL/GOOGLE_SIGN_IN_ENABLED, GOOGLE_WEB_CLIENT_ID, supabase.* keys, google-services.json); `/kit-setup-firebase` (google-services.json + plugin application + package_name match); `/kit-setup-paywall` (PAYWALL_ENABLED, ENTITLEMENT_ID, PAYWALL_MODE, revenuecat.android.api.key); `/kit-setup-review-dialog` (InAppReviewManager.requestReview call-sites + reviewPrompted DataStore key); `/kit-setup-theme` (BrandPrimary hex, LocalKitIcons impl, composeicons-* deps uncommented); `/kit-setup-updates` (REMOTE_CONFIG_PROVIDER + provider-specific creds).
- **Supabase + Google sign-in = TWO Google Cloud OAuth clients (locked 2026-06-02)** — `/kit-setup-auth` Step 4a Google branch correctness fix. The Web client is the OAuth client Supabase uses to talk to Google (Authorized redirect URI = `https://<project-id>.supabase.co/auth/v1/callback`; Client ID + Secret pasted INTO Supabase + Client ID also used as `KitConfig.GOOGLE_WEB_CLIENT_ID`). The Android client is what the device uses for native Credential Manager sign-in (package name = applicationId + debug SHA-1). Earlier copy wrongly implied Supabase generates the Client ID — Supabase doesn't, Google Cloud Console does, and you paste it INTO Supabase. Release-time = a second Android client w/ release SHA-1 (handled by `/kit-upload-on-google-play` Step D).
- **/kit-upload-on-google-play Step D = release SHA-1 register (added 2026-06-02)** — release-time registration of the Play release SHA-1 for Google sign-in. Detects `KitConfig.GOOGLE_SIGN_IN_ENABLED` + `AUTH_PROVIDER`; skips if Google sign-in is off OR provider is STUB. Branches Play App Signing (SHA-1 from Play Console → App integrity, after first upload) vs self-managed keystore (Claude runs `keytool -list -v` after explicit "yes"). Then walks Supabase path (Google Cloud Console new Android (release) OAuth client) vs Firebase path (fingerprint added in Firebase Console + re-download `google-services.json` + Claude overwrites `app/google-services.json` from dev-supplied path). First-version path only; update path skips (release SHA-1 = one-time).
- **/kit-setup-paywall = paced + release-time block (revised 2026-06-02)** — RevenueCat dashboard moved Apps + API keys from "Project Settings" to top-level sidebar items. Step 2 rewritten into 6 paced sub-steps: 2.1 create project; 2.2 sidebar Apps → New app configuration → Google Play Store (App name + Google Play package name + Service Account JSON deferred to 2.6); 2.3 Product catalog → Entitlements → premium; 2.4 sidebar API keys → copy `goog_...` → write to `local.properties`; 2.5 pick SOFT/HARD + flip `KitConfig.PAYWALL_ENABLED = true`; 2.6 release-time info block (read now, do later): Play Console products + service account JSON via https://www.revenuecat.com/docs/platform-resources/google-platform-resources + offerings + paywall design in RC's visual editor. References https://www.shipkaro.dev/mobile-docs/features/in-app-purchases for additional step-by-step.
- **/kit-start-setup = 7 steps, no Ops (revised 2026-06-02)** — Step 7 Ops (remote config / update gate / push) dropped. For a non-coder shipping v1, the decisions are theoretical (no users yet). Kit ships `RemoteAppConfig.LOCAL` by default so the gate compiles + runs offline. Step 8 Build promoted to Step 7. Outro lists the 7 optional commands devs can run later (`/kit-env-check`, `/kit-setup-updates`, `/kit-setup-review-dialog`, `/kit-setup-ai`, `/kit-translate`, `/kit-design-onboarding`, `/kit-design-app`, `/kit-upload-on-google-play`). `/kit-setup-updates` stays standalone, unchanged. Drove from "don't overwhelm beginners" rule.
- **/kit-env-check = cross-platform per-machine probe (added 2026-06-02)** — separate command for per-machine env (JDK 17, `ANDROID_HOME`, adb, android CLI, Android Skills + optional scrcpy + Holo) vs `/kit-start-setup` Step 0's per-project setup (`local.properties` + SDK path + Gradle wrapper). `uname` branches install commands per OS: macOS (brew + curl|sh), Windows (Scoop + PowerShell SetEnvironmentVariable + cargo install holo because the holo install.sh is bash-only), Linux (apt + curl|sh). References `shipkaro.dev/mobile-docs/getting-started/setup-macos` + `.../setup-windows`. Wire-ins: `/kit-start-setup` Step 0 prereq top + outro optional-commands list. Holo (measure-sh/holo) added as recommended terminal-first Android profiler for the cohort audience that skips Android Studio.
- **Sentry auto-init disabled (added 2026-06-02)** — `<meta-data android:name="io.sentry.auto-init" android:value="false" />` in `<application>`. Sentry's `SentryInitProvider` ContentProvider runs at process start (before `KitApplication.onCreate`); without a DSN it throws `IllegalArgumentException("DSN is required")` and crashes the app. KitApplication's manual `SentryAndroid.init` (gated on `SENTRY_ENABLED` + non-blank `BuildConfig.SENTRY_DSN`) only runs once you reach `onCreate`, by which point the crash already fired. Auto-init opt-out moves init to KitApplication exclusively.
- **OSS Licenses = mikepenz/AboutLibraries, NOT Google oss-licenses-plugin (revised 2026-06-02)** — Google's `oss-licenses-plugin` only scans Google Play Services / Firebase artifacts; non-Google deps (Compose, Retrofit, Koin, Coil, Timber, RevenueCat, Supabase, etc.) don't publish the `third_party_licenses` resource it relies on, so the screen rendered blank. Swapped to `com.mikepenz.aboutlibraries.plugin:11.6.2` + `aboutlibraries-compose-m3:11.6.2`. Plugin scans the full Gradle dep tree at build time → emits `aboutlibraries.json` asset; `LibrariesContainer` composable renders it w/ Material 3 styling. New `feature/licenses/OssLicensesScreen.kt` drops it into a Scaffold; `Route.OssLicenses` wired in `KitNavHost` (post-auth aware); `SettingsScreen` takes a new `onOssLicenses` callback. Removed `play-services-oss-licenses` lib + `oss-licenses` plugin + `settings.gradle.kts` plugin-resolution mapping.
- **SwiftStarterKits parity scope (added 2026-06-02)** — 20-component expansion to close the gap with SwiftStarterKits.com's "100+ SwiftUI components" claim. Mapped each iOS component to its Android equivalent via Material 3, skipping iOS-only items (Apple sign-in, dynamic app icons — already rejected, Liquid Glass / SF symbol specifics). Shipped pickers (Slider, Stepper, SegmentedButton, DatePicker, DropdownMenu, SearchField, OtpField), list patterns (SwipeableRow, DisclosureGroup, LazyGrid), feedback (PullToRefresh, LinearProgress, Shimmer + Skeleton), misc UI (Badge, RatingBar, FAB), scaffolds (BottomNavScaffold + BottomNavBar), WebView (JS-off default; prefer CustomTabs for external), utils (EmailLauncher, KitHaptics), and Snackbars w/ 4 severity styles (TonnyL/Light-inspired API: `controller.success("Saved!")`). Future similar passes (e.g. ShipNative parity) follow the same flow: map → cull iOS-only → ship → live-preview in catalog.
- **Snackbar API = Light-inspired severity helpers (added 2026-06-02)** — `KitSnackbarController` returned by `rememberKitSnackbarController()` exposes `.info() / .success() / .warning() / .error()` so call sites read like the intent. Backed by `KitSnackbarVisuals` (SnackbarVisuals subclass carrying style + title). `KitSnackbarHost` renders these w/ matching `KitTheme.colors` + `KitIcons`, falls back to Material default for plain strings — unrelated callers still work. Error duration LONG, others SHORT. Action callback only fires on `ActionPerformed`. Previous snackbar auto-dismissed when a new one shows so rapid `success()` calls don't queue.
- **Components catalog screen (added 2026-06-01)** — `feature/catalog/ComponentsCatalogScreen.kt`. Live preview of every kit component, grouped by section. Reachable from Home → "Browse kit components". Purpose: post-setup sanity-check so the dev sees their brand-color + icon-pack picks reflected across the whole design system before they start building features. Pure dummy data; non-interactive (every onClick is no-op or local state) — safe to keep in a shipped app or delete in three places (screen file + `Route.ComponentsCatalog` + Home button). Not gated by a `KitConfig` flag — its existence is the kit's documentation, not a feature.
- **Sentry crash reporter (added 2026-06-01)** — `io.sentry:sentry-android:7.20.0` + new `core/log/SentryTree.kt` (Timber.Tree mirroring INFO/WARN to breadcrumbs, ERROR/ASSERT to `captureException` / `captureMessage`). Runs **alongside** `CrashlyticsTree` when both are picked — Timber supports any number of trees. `KitConfig.SENTRY_ENABLED` (default false) + `BuildConfig.SENTRY_DSN` (from `local.properties`). Inert until both are set. `/kit-setup-analytics` Step 1 multi-select now includes Sentry; new Step 4 walks the DSN paste flow.
- **OSS licenses screen (added 2026-06-01)** — `com.google.android.gms:oss-licenses-plugin:0.10.6` + `play-services-oss-licenses:17.1.0`. Plugin scans POMs at build time → bakes licenses into raw resources → Settings → About → "Open source licenses" launches `OssLicensesMenuActivity` with a localized title. Plugin has no plugin-marker artifact, so `settings.gradle.kts` maps the ID to the classpath artifact via `resolutionStrategy.eachPlugin`.
- **GitHub backup = `/kit-save-to-github` via `gh` CLI (added 2026-06-11)** — closes the gap where docs said "clone the kit" but nothing helped a non-coder push their *new app* to their own GitHub. Agent-driven: detects kit-origin vs user-origin, ensures `gh auth login`, first-time = detach kit origin + `gh repo create --private --source=. --push`, re-save = add/commit/push. Offered as **AskUserQuestion** (not a paragraph) at the end of `/kit-start-setup` AND standalone. `gh` is an **optional** tool in `/kit-env-check`. Optional + not required to ship to Play (Play takes an AAB, not a repo). GUI fallback = GitHub Desktop, mentioned only if asked.
- **Icons ship Material-only by default (revised 2026-06-11, supersedes "feather+tabler shipped")** — Feather + Tabler are in the repo but **disabled** out of the box: `composeicons-feather/tabler` commented in `libs.versions.toml` + `app/build.gradle.kts`, and `FeatherKitIcons.kt`/`TablerKitIcons.kt` bodies wrapped in `/* */` (Kotlin nested block comments are legal, so the inner `/* */` in the explainer is fine). A fresh clone carries no unused icon-pack deps. `/kit-setup-theme` flipped: **Material pick = no-op**; **Feather/Tabler pick = enable** (uncomment dep in catalog + build, unwrap impl, set `LocalKitIcons` binding); the old "Apply/disable unused" is now the reverse-on-switch helper.
- **Paywall billing: BILLING permission + lead-time-start-early (added 2026-06-11)** — `com.android.vending.BILLING` lives in AndroidManifest **commented + paywall-gated**: `/kit-setup-paywall` uncomments it when enabling the paywall, a free app keeps it commented (confirmed via RevenueCat docs: Play won't unlock in-app product creation without it in the uploaded build). The two slow billing clocks (RevenueCat↔Play service-account JSON **~36 h** propagate; a build on an **internal testing track** so products load + first review) are startable right after first run via **`/kit-start-setup` Step 8** (paywall-only, AskUserQuestion start-now/later). `/kit-upload-on-google-play` **A.1** rechecks all of it before shipping. Sub-step 2.6 of `/kit-setup-paywall` is now/later (not a wall of text); full reference = `/docs/paywall`. **Rejected** a `DEBUG_FORCE_PREMIUM` toggle (user: a workaround vibe coders would miss before launch); use a RevenueCat **Promotional entitlement** to test premium UX without buying.
- **Setup wrap-up must stay short (added 2026-06-11)** — `/kit-start-setup`'s end screen overwhelmed a vibe coder (huge scroll). Rule: GitHub backup = AskUserQuestion (options carry the why), and the optional-commands list is **one line each** (name + ≤6-word hint), never multi-line descriptions. Same anti-overflow rule applies to any command's verbatim blocks — don't make a non-coder scroll through command explanations they didn't ask for.

## Known Issues / Blockers
- [x] ~~detekt root-only → NO-SOURCE~~ — RESOLVED Phase 5: detekt plugin applied to `:app`, config Compose-tuned.
- [x] ~~Retrofit baseUrl hardcoded placeholder~~ — RESOLVED 2026-05-22: hoisted to `KitConfig.API_BASE_URL` (still a placeholder value — set real host before shipping).
- [x] ~~`KitDatabase.fallbackToDestructiveMigration()` deprecation~~ — RESOLVED 2026-05-22: swapped to the `dropAllTables = true` overload.
- [x] ~~`KitConfig.AUTH_PROVIDER` shipping default~~ — RESOLVED 2026-05-22: set to `STUB` (builds + runs with no backend creds).
- [x] ~~`.debug` `applicationIdSuffix` breaking Firebase~~ — RESOLVED 2026-05-30: suffix removed; debug + release builds share one applicationId.
- [x] ~~Demo confuses no-auth/no-paywall devs~~ — RESOLVED 2026-05-30: demo nuked entirely; `HomeScreen` now shows System Status panel.
- [x] ~~Settings shows sign out / delete account when `AUTH_ENABLED=false`~~ — RESOLVED 2026-05-30: account section + AccountRow gated by `AUTH_ENABLED`.
- [x] ~~Urdu strings bundled despite never being requested~~ — RESOLVED 2026-05-30: `values-ur/` deleted; English-only by default; `/kit-translate` adds locales on demand.
- [x] ~~Free-app paywall still showed after onboarding~~ — RESOLVED 2026-05-31: `/kit-start-setup` Step 5 free-app branch flips `PAYWALL_ENABLED = false`.
- [x] ~~Dark theme stayed purple after brand color change~~ — RESOLVED 2026-05-31: `/kit-setup-theme` Step 1 derives 6 container/dark variants from `BrandPrimary` via HSL.
- [x] ~~Splash showed `ic_shipkaro_mark` placeholder arrow + launcher icon was AOSP eject-icon~~ — RESOLVED 2026-05-31: real ShipKit logo PNG replaces the vector; adaptive-icon foreground insets the same PNG by 19 %; `ic_launcher_background` uses `BrandPrimary`.
- [x] ~~Delete-account left a blank screen~~ — RESOLVED 2026-05-31: race between `KitNavHost` sign-out redirect and `SettingsScreen` `LaunchedEffect(Done) { onBack() }`. Removed the redundant `onBack()`; redirect alone handles navigation.
- [x] ~~Two splashes (Android OS + in-app 1.5s SplashScreen)~~ — RESOLVED 2026-05-31: in-app `SplashScreen` + `Route.Splash` deleted. `KitNavHost` resolves the start route on initial composition under a blank Box so the OS splash stays visible.
- [x] ~~Stub auth shown as a setup option for prod apps~~ — RESOLVED 2026-05-31: dropped from `/kit-setup-auth` question; `AUTH_ENABLED` default flipped to false; Stub stays as internal fallback only.
- [ ] `release` buildType produces an UNSIGNED AAB locally (no keystore); CI signs via `RELEASE_*` env vars. Kit author wires their own keystore.
- [ ] **Device-test of 2026-06-01 + 2026-06-02 work pending** — Timber + Crashlytics breadcrumbs, Sentry tree (when DSN configured), rate-button → Play listing, OSS licenses screen, Components catalog screen + 7 new catalog sections + 20 new components (Snackbar 4 styles, pickers, list patterns, shimmer/skeleton, badge/rating, FAB + BottomNav, WebView, email/haptics utils), `/kit-setup-ai`, `/kit-setup-review-dialog`, `/kit-plan-release-analytics`, `/kit-generate-legal`. `:app:assembleDebug` green; runtime verification is the user's next action.
- [ ] **Phase 7 sales/docs website** — not started. Separate Next.js+Nextra repo per `.session/phase7-sales-and-docs-plan.md`.
- [ ] **Terminal-first setup branching deferred** — `/kit-start-setup` should ask "Android Studio available vs terminal-only?" once in Step 0 and branch all steps. Not yet built.

## Commands to Remember
- Build debug APK: `./gradlew :app:assembleDebug --no-daemon`
- Compile only: `./gradlew :app:compileDebugKotlin`
- Lint (after Phase 5 wiring): `./gradlew detekt`
- Gradle wrapper = 8.14.5; do NOT run system `gradle` (8.5, too old for AGP)
- Add a language: values-XX/strings.xml + resourceConfigurations (app/build.gradle.kts) + res/xml/locales_config.xml

## Daily Workflow
- Session start: "Read CLAUDE.md and tell me where we left off."
- Session end: "Update CLAUDE.md: mark completed, add decisions, update session log."
